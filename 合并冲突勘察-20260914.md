# 上游 merge 冲突勘察（2026-09-14）

> **性质**：只读勘察报告，**不改 main、不 commit、不 push**。
> **方法**：从 `main`(=c8e5c04) 建临时分支 `merge/upstream-recon-20260914`，执行
> `git merge --no-commit --no-ff upstream/main`（= `c3a95ba`），收集冲突清单后
> `git merge --abort`，删分支，回到干净状态。全程未提交合并结果。
> **merge-base**：`4cbb2a2`（fork-base，grafted）。

---

## 一、总览

| 指标 | 值 |
|---|---|
| 上游待并入 commit | 187（`4cbb2a2..upstream/main`） |
| 我方 side 改动文件数（`4cbb2a2..main`） | 419 |
| 上游 side 改动文件数（`4cbb2a2..upstream/main`） | 1308 |
| **试合并冲突总数（`--diff-filter=U`）** | **73** |
| 冲突状态码分布 | `UU` 71 · `DU` 1 · `UD` 1 |

> 合并命令返回非 0、提示 "Automatic merge failed; fix conflicts and then commit the result."（预期）。

---

## 二、冲突分类清单（73）

### 1. locales 翻译 — 35
`apps/readest-app/public/locales/<L>/translation.json`，L =
ar, bn, bo, de, el, en, es, fa, fr, he, hi, hu, id, it, ja, ka, ko, ms, nl, pl, pt-BR, pt, ro, ru, si, sl, sv, ta, th, tr, uk, uz, vi, zh-CN, zh-TW

### 2. src-tauri 打包 / 署名 — 4
- `apps/readest-app/src-tauri/Info-ios.plist`
- `apps/readest-app/src-tauri/Info.plist`
- `apps/readest-app/src-tauri/gen/android/app/build.gradle.kts`
- `apps/readest-app/src-tauri/gen/android/app/src/main/AndroidManifest.xml`

### 3. reader — 5
- `apps/readest-app/src/app/reader/components/annotator/ExportMarkdownDialog.tsx`
- `apps/readest-app/src/app/reader/components/sidebar/BooknoteItem.tsx`
- `apps/readest-app/src/app/reader/hooks/kosyncProgress.ts`
- `apps/readest-app/src/app/reader/hooks/useKOSync.ts`
- `apps/readest-app/src/app/reader/hooks/useProgressSync.ts`

### 4. auth — 2
- `apps/readest-app/src/app/auth/components/AuthPanel.tsx`（UU）
- `apps/readest-app/src/app/auth/components/ProviderLogin.tsx`（**DU：我方删除 / 上游修改**）

### 5. library — 4
- `apps/readest-app/src/app/library/components/ImportFromUrlDialog.tsx`（**UD：我方修改 / 上游删除**）
- `apps/readest-app/src/app/library/components/ImportNovelDialog.tsx`
- `apps/readest-app/src/app/library/hooks/useBooksSync.ts`
- `apps/readest-app/src/app/library/page.tsx`

### 6. user — 1
- `apps/readest-app/src/app/user/page.tsx`

### 7. integrations 设置表单 — 5
- `apps/readest-app/src/components/settings/integrations/BookOrbitForm.tsx`
- `apps/readest-app/src/components/settings/integrations/KOSyncForm.tsx`
- `apps/readest-app/src/components/settings/integrations/LocalSendForm.tsx`
- `apps/readest-app/src/components/settings/integrations/S3Form.tsx`
- `apps/readest-app/src/components/settings/integrations/SendToReadestForm.tsx`

### 8. pages/api — 4
- `apps/readest-app/src/pages/api/send/address.ts`
- `apps/readest-app/src/pages/api/send/senders.ts`
- `apps/readest-app/src/pages/api/storage/stats.ts`
- `apps/readest-app/src/pages/api/storage/upload.ts`

### 9. services — 2
- `apps/readest-app/src/services/runtimeConfig.ts`
- `apps/readest-app/src/services/transferManager.ts`

### 10. hooks — 1
- `apps/readest-app/src/hooks/useQuotaStats.ts`

### 11. utils — 1
- `apps/readest-app/src/utils/xcfi.ts`

### 12. __tests__ — 5
- `apps/readest-app/src/__tests__/app/reader/annotator/ImportAnnotationsDialog.test.tsx`
- `apps/readest-app/src/__tests__/hooks/useProgressSync.test.tsx`
- `apps/readest-app/src/__tests__/services/send-address-plan-gate.test.ts`
- `apps/readest-app/src/__tests__/services/sync/providers/s3/S3Provider.test.ts`
- `apps/readest-app/src/__tests__/services/translators/providers.test.ts`

### 13. 根级 / 其他 — 4
- `README.md`
- `apps/readest-app/package.json`
- `apps/readest.koplugin/main.lua`
- `data/metainfo/appdata.xml`

---

## 三、重点标注项结论（team-lead 指定核验）

| 重点文件 | 是否冲突 | 说明 |
|---|---|---|
| `apps/readest-app/src/middleware.ts` | **否** | 两侧自 `4cbb2a2` 起均**未改动**该文件。 |
| `docker/**` | **否** | 全仓 `docker/` 无冲突（`Dockerfile`、`docker/compose.yaml`、`docker/.env.example` 等均干净）。 |
| `apps/readest-app/src/app/opds/components/CatalogManager.tsx` | **否** | 我方 N10 改动与上游无重叠。 |
| `apps/readest-app/src/app/auth/**` | **是（2）** | AuthPanel.tsx（UU）、ProviderLogin.tsx（DU：我方删 OAuth / 上游改）。 |
| `apps/readest-app/src/libs/payment/**` | **否** | 我方改 `epay.ts`，上游改 `iap/*`、`storage.ts`、`stripe/client.ts` —— **无文件重叠**，干净合并。 |
| `apps/readest-app/src-tauri/tauri.conf.json` | **否** | 两侧无重叠改动。 |
| `apps/readest-app/src-tauri/Info.plist` | **是** | 见 §二.2。 |

### ⚠️ 重要修正：10MB 上传修复（`731b13a`）落点不是 middleware.ts
`731b13a`（`fix(docker): unbreak large uploads and self-hosted cloud sync (#6100)`）实际改动：
`Dockerfile`、`apps/readest-app/next.config.mjs`、`IntegrationsPanel.tsx`、`cloudSyncStatus.ts`、
`cloudSyncStatus.test.ts`、`docker-image-defaults.test.ts`、`next-config.test.ts`、`docker/README.md`。
以上文件 **全部无冲突**（`IntegrationsPanel.tsx` / `cloudSyncStatus.ts` 我方各改 1 处，但与上游改动不重叠）。
→ **结论：该 10MB 修复可随本次合并干净吸收，无需人工干预。**

---

## 四、可脚本化 vs 必须人工（初判）

| 处理方式 | 数量 | 文件 |
|---|---|---|
| **可脚本化批量** | 35 | 全部 `locales/*/translation.json`。冲突多为同一 key 两侧都改：我方"品牌串"（Readest→墨阅/Moyue 等）vs 上游新增/修订文案。建议：**以 `upstream/main` 的 JSON 为结构基准，脚本重放我方品牌替换规则**，产出后抽检 diff。 |
| **半脚本化** | 1 | `apps/readest-app/package.json`：依赖/脚本字段，脚本取"两侧并集 + 版本取上游"后**必须人工校验**。 |
| **必须人工** | 37 | src-tauri(4)、reader(5)、auth(2)、library(4)、user(1)、integrations(5)、pages/api(4)、services(2)、hooks(1)、utils(1)、__tests__(5)、README.md(1)、readest.koplugin/main.lua(1)、data/metainfo/appdata.xml(1) |

**人工项优先关注（风险最高）**：
1. `auth/**`（我方删 OAuth 的取舍；ProviderLogin 保留删除）
2. `library/components/ImportFromUrlDialog.tsx`（UD：上游删、我方改 —— 需决策保留或随上游删）
3. `reader/hooks/*` 与 `services/*`（KOSync / 进度同步 / runtimeConfig / transferManager —— 业务逻辑冲突）
4. `src-tauri/Info.plist` / `AndroidManifest.xml`（品牌署名 + 权限）
5. `apps/readest-app/package.json`（依赖版本）

---

## 五、勘察证据（可复现）

```sh
git switch -c merge/upstream-recon-20260914          # 从 main(c8e5c04)
git merge --no-commit --no-ff upstream/main          # 返回 1，产生 73 冲突
git diff --name-only --diff-filter=U | sort          # = §二 清单（73）
git status --short | grep -E '^(UU|AA|DU|UD) ' | wc -l   # 73
git merge --abort && git switch main && git branch -D merge/upstream-recon-20260914
# 恢复后 git status 仅 3 个有意 untracked；HEAD=c8e5c04，与 origin/main 一致
```

> 上游 ref：`upstream/main` = `c3a95ba7b35c013baa644358669d28bd55efaba8`。
