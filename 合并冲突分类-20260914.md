# 上游 merge 冲突分类（2026-09-14）

> **性质**：只读分析（不开 merge / 不建分支 / 不提交 / 不 push），供制定合并执行规格。
> **输入**：`合并冲突勘察-20260914.md` 的 73 处冲突（`git diff 4cbb2a2..main`）。
> **方法**：对每个冲突文件做**对称品牌归一化**——把两侧所有品牌变体（Readest/readest/READEST/Moyue/moyue/MOYUE/墨阅/Bilingify/com.bilingify.readest/com.moyue.app）统一替换为占位符后比对：
> - `norm(base) == norm(ours)` → **A 纯改名**
> - 归一化后我方仅**新增行** → **A+**
> - 归一化后仍有**非品牌增删改** → **C 功能型**
> （另结合逐文件 diff 人工复核。)

---

## 一、三类数量统计

| 类别 | 数量 | 含义 | 处理方式 |
|---|---|---|---|
| **A 纯改名** | **52** | 我方改动 = 品牌改名，其余与 base 逐字节相同 | 取上游版本 + 重放品牌改名（机械） |
| **A+ 改名+新增** | **5** | 品牌改名 + 我方新增内容（i18n 键 / 新 hook / 新字段） | 取上游 + 重放改名 + 叠加我方新增（union） |
| **B 独立新增** | **2** | 双方各自独立新增，无品牌因素 | 并集（union） |
| **C 功能型** | **13** | 我方改了行为/接口/逻辑，与上游同区域重叠 | **必须人工** |
| **特殊：删除冲突** | **1** | `ProviderLogin.tsx`（DU：我方删 / 上游改） | 保留删除（见 §五） |
| 合计 | 73 | | |

---

## 二、A 类完整清单（52，纯改名 → 机械可解）

### A-1 翻译（32）
`apps/readest-app/public/locales/<L>/translation.json`：
ar, bn, bo, de, el, es, fa, fr, he, hi, hu, id, it, ja, ka, ko, ms, nl, pl, pt-BR, pt, ro, ru, si, sl, sv, ta, th, tr, uk, uz, vi

### A-2 代码/配置（20，均"我方改动=纯品牌"）
- `apps/readest-app/src-tauri/Info-ios.plist`
- `apps/readest-app/src-tauri/Info.plist`
- `apps/readest-app/src/__tests__/app/reader/annotator/ImportAnnotationsDialog.test.tsx`
- `apps/readest-app/src/__tests__/hooks/useProgressSync.test.tsx`
- `apps/readest-app/src/__tests__/services/sync/providers/s3/S3Provider.test.ts`
- `apps/readest-app/src/__tests__/services/translators/providers.test.ts`
- `apps/readest-app/src/app/library/components/ImportFromUrlDialog.tsx` ← **同时是 UD 项（上游删除）**
- `apps/readest-app/src/app/library/components/ImportNovelDialog.tsx`
- `apps/readest-app/src/app/library/hooks/useBooksSync.ts`
- `apps/readest-app/src/app/reader/components/annotator/ExportMarkdownDialog.tsx`
- `apps/readest-app/src/app/reader/components/sidebar/BooknoteItem.tsx`
- `apps/readest-app/src/app/reader/hooks/kosyncProgress.ts`
- `apps/readest-app/src/app/reader/hooks/useKOSync.ts`
- `apps/readest-app/src/app/reader/hooks/useProgressSync.ts`
- `apps/readest-app/src/components/settings/integrations/BookOrbitForm.tsx`
- `apps/readest-app/src/components/settings/integrations/KOSyncForm.tsx`
- `apps/readest-app/src/components/settings/integrations/LocalSendForm.tsx`
- `apps/readest-app/src/components/settings/integrations/S3Form.tsx`
- `apps/readest-app/src/services/transferManager.ts`
- `apps/readest-app/src/utils/xcfi.ts`

> 注：`BooknoteItem.tsx` 含一处 prettier 折行（3 行→1 行），语义仍是纯改名。

---

## 三、A+ 类完整清单（5，改名+我方新增）

| 文件 | 我方新增内容 | 上游是否同区域改 |
|---|---|---|
| `apps/readest-app/public/locales/en/translation.json` | **仅新增 7 键**（无改名）：`catalog.wikisource-zh.name/.description`、`catalog.wikisource-en.name/.description`、`catalog.archive-publicdomain.name/.description` + 前一键补尾逗号 | 是（上游新增键） |
| `apps/readest-app/public/locales/zh-CN/translation.json` | 改名 59 处 + **新增 35 键**（N08 邀约/会员/支付 + N10 catalog.*） | 是 |
| `apps/readest-app/public/locales/zh-TW/translation.json` | 改名 59 处 + **新增 35 键**（同上，繁体） | 是 |
| `apps/readest-app/src/app/library/page.tsx` | 改名 + 新增 2 行（`import { useOpenRedeemLink }` + `useOpenRedeemLink();`，N08 `moyue://redeem` 深链） | 是 |
| `apps/readest-app/src/services/runtimeConfig.ts` | 改名（注释）+ 新增 `paymentProvider` 字段（epay/stripe 运行时切换） | 是（同 interface 区域加字段） |

---

## 四、B 类清单（2，独立新增，需并集）

| 文件 | 我方 | 上游 |
|---|---|---|
| `apps/readest-app/package.json` | 仅 `version` 0.12.1 → **0.1.0**（无其他） | 52 行（依赖/脚本升级） |
| `data/metainfo/appdata.xml` | 追加 0.1.0 release 条目（15 行 release notes） | 追加/修改 release 条目 |

> 建议：两者都"取上游 + 保留我方新增（version 0.1.0 / 0.1.0 release 条目）"。**版本策略（0.1.0 vs 上游 0.12.x）需你决策**。

---

## 五、C 类完整清单 + 逐文件摘要（13，必须人工）

> 格式：`<文件>` — 我方改了什么（关键 hunk 行号）｜上游是否动同一处。

1. **`README.md`** — 我方：把上游 README **整套替换**为"墨阅（Moyue）· 接手人速览"（−286/+84，非改名）｜上游：同文件小改 14 行。→ **内容互斥**；属文档，建议**保留我方**、弃上游 README 改动（无功能风险）。
2. **`apps/readest-app/src-tauri/gen/android/app/build.gradle.kts`** — 我方：把 `keystoreProperties` 加载块前移（@16 新增 6 行）+ `namespace` 品牌 + 签名配置（keystore.properties 驱动）｜上游：`android {}` 内 @26 改 8 行。→ **同一 android{} 区域**，半机械（品牌+布局 vs 上游改动）。
3. **`apps/readest-app/src-tauri/gen/android/app/src/main/AndroidManifest.xml`** — 我方：重排/收敛权限块（@3，−13/+1，品牌相关）｜上游：3 处（+1 权限 / −1 权限 / 改 1）。→ **同一权限区**。
4. **`apps/readest-app/src/app/auth/components/AuthPanel.tsx`** — 我方：**移除 OAuth**（删 `ProviderLogin` import、`onProviderSignIn` prop、Google/Apple/GitHub/Discord 按钮，@3/@6/@13/@20/@35）+ `Sign in to Readest`→Moyue 改名｜上游：@9/@25/@48/@54 改 25 行（同一组件区）。→ **我方删 OAuth，需人工保删除策略**。
5. **`apps/readest-app/src/app/user/page.tsx`** — 我方：N08 兑换/支付 UI（@4/@17/@48/@59：`EpayCheckout` 类型、引入 `RedemptionCard`、`getRuntimeConfig`/`MEMBERSHIP_PLANS`）｜上游：@18/@35/@95/@134 改 24 行（ProfilePage 同区域）。→ 冲突。
6. **`apps/readest-app/src/components/settings/integrations/SendToReadestForm.tsx`** — 我方：plan 解析改为**服务端**（`getUserProfilePlan`→`/api/user/plan`，@10/@73/@77）｜上游：@10/@54/@74/@76 改 22 行（**同一 plan 逻辑**）。→ 冲突。
7. **`apps/readest-app/src/hooks/useQuotaStats.ts`** — 我方：plan/配额改为**服务端解析**（`getTranslationQuota`/`getDailyUsage`/`getAPIBaseUrl`，@1/@4/@16）｜上游：@1/@4/@12/@15 改 39 行（**同一逻辑**）。→ 冲突。
8. **`apps/readest-app/src/pages/api/send/address.ts`** — 我方：改用 `getUserPlanData`（`@/utils/plan`，@4/@38）｜上游：@6/@45 改 5 行。→ 同文件。
9. **`apps/readest-app/src/pages/api/send/senders.ts`** — 我方：同上 `getUserPlanData`（@4/@30）｜上游：@6/@36 改 5 行。
10. **`apps/readest-app/src/pages/api/storage/stats.ts`** — 我方：quota 取自 plans 表 + `usage=totalSize`（@4/@66）｜上游：@65 改 10 行（同区域）。
11. **`apps/readest-app/src/pages/api/storage/upload.ts`** — 我方：服务端配额判定（@4/@90）｜上游：@93/@99 改 20 行（**同一配额逻辑**）。
12. **`apps/readest.koplugin/main.lua`** — 我方：`SUPABAE_ANON_KEY_BASE64` 换自建 + `supabase_url`→`https://readest-api.chen-cn.top`（@29/@32）｜上游：162 行大重写。→ 我方配置改动 vs 上游重写。
13. **`apps/readest-app/src/__tests__/services/send-address-plan-gate.test.ts`** — 我方：mock 由 `getUserProfilePlan`→`getUserPlanData`（`@/utils/plan`，@9/@12/@17/@20/@93）｜上游：@9/@18/@91/@101/@107 改 51 行（**同一逻辑**）。→ 冲突。

**C 类共性**：8–11、13 是同一"plan 解析从 JWT claim 改为服务端 `/api/user/plan`（plans 表）"的改造（N08 会员链路）；6、7 是其在 UI 层的镜像。**这批是同一主题，建议一起人工合并**。

---

## 六、UD 项结论

**`apps/readest-app/src/app/library/components/ImportFromUrlDialog.tsx`（UD：上游删除 / 我方修改）**
- 我方改动：**纯品牌改名**（对称归一化后与 base 逐字节一致，已实测 `identical after rename: True`）。
- 上游删除于 `62b0162 feat: unify web imports with signed-in page clipping (#6117)`——把网页导入统一为"登录后页面剪藏"流程。
- **建议：接受上游删除**。我方此处无任何功能改动，删除不丢 fork 功能。

## 「DU」项结论（附）
**`apps/readest-app/src/app/auth/components/AuthPanel.tsx` / `ProviderLogin.tsx`**
- `ProviderLogin.tsx` = **DU（我方删除 / 上游修改）**：我方随 OAuth 下线删除该组件 → **保留删除**，丢弃上游对该文件的改动。
- 关联 `AuthPanel.tsx` 归 **C**（见 §五.4）。

---

## 七、品牌替换规则集（最终，供批量脚本）

| # | 规则 | 语境 / 例外 |
|---|---|---|
| 1 | `Readest` → `Moyue` | 拉丁/英文 UI；**zh-CN / zh-TW 语境改为 `墨阅`** |
| 2 | `readest` → `moyue` | 小写标识 / URL scheme（`readest://`→`moyue://`） |
| 3 | `READEST` → `MOYUE` | 全大写（如环境变量名） |
| 4 | `com.bilingify.readest` → `com.moyue.app` | reverse-DNS（tauri identifier / DBUS_ID / CFBundleURLName / iCloud 容器） |
| 5 | `bilingify` → `moyue` | 公司名 |
| 6 | `readest-onedrive` → `moyue-onedrive` | OAuth scheme |

### ⚠️ 关键：**不能一把全局 sed**（改名是"选择性"的）
实测确认我方**未**改动的品牌串（必须保留）：
- 代码标识符：`isReadestCloudEnabled`、`isReadestCloudStorageActive`、`onImportReadest`、`ReadestSync`、`ReadestRuntimeConfig`、`Readest.debounce` 等——**保留**。
- Sentry 标识：`READEST-2` 等注释——**保留**。
- 存储桶 / 域：R2 bucket 段 `readest`、域名 `readest.chen-cn.top` / `readest-api.chen-cn.top`——**保留**（改名 commit 未动）。
- 测试断言里"半改名"：`S3Provider.test.ts` 只把 `'/Readest/'`→`'/Moyue/'` 文件夹段，bucket 名 `readest` 不动；`ImportAnnotationsDialog.test.tsx` 只改显示串 `'Readest'`→`'Moyue'`，标识符 `onImportReadest` 不动。

→ **批量脚本必须限定范围**（仅字符串字面量/注释/配置值），或改为"以上游为基线、逐冲突人工重放我方 `git diff 4cbb2a2..main` 的改名 hunk"。**建议后者更稳**：A 类文件直接 `git checkout --theirs <file>`（= 上游版本）后对那几十处 hunk 重放改名。

---

## 八、可脚本化 vs 必须人工（结论）

- **机械可解（上游 + 重放改名）**：A 52 + A+ 5 = **57**
- **并集**：B 2
- **必须人工**：C 13 + DU/删除 1 = **14**
- 最高风险人工项：auth（OAuth 删除）、plan/配额系列（#6–11,#13）、main.lua、README、package.json（版本策略）、build.gradle.kts/AndroidManifest。
