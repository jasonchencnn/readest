# 需求 N08 · 兑换码支付（替代第三方支付）

> **本文件是 N08 节点的唯一设计文档**。实现时按本文件执行；偏离时先回头改本文件。
> 关联：`项目主控.md` N08 详细说明 / `墨阅项目规则.md`（合规 + 不动数据目录）/ `DEPLOY.md`（DB 迁移部署）。

---

## 一、为什么改方案

原计划：接 YunGouOS 或虎皮椒（第三方支付服务商），让用户扫码支付。

**问题**：
- 央行 2022 新规：个人收款码不得经营性收款
- 虎皮椒：开户费 ¥118+¥88 = ¥206 + 平台费 1-2% + 官方 0.6% = **总成本 1.6-2.6%**
- YunGouOS：开户费 ¥200 起 + 接口 0-1% + 官方 0.38% 起 = **总成本 0.4-1.4%**（个人档偏高）
- 两者都涉及"经营性收款"灰区；月流水几百块时固定成本占比偏高
- 未来 N09 合规路径不变

**新方案**：**完全绕开第三方支付接口**，改为**兑换码**模式。

---

## 二、新方案 · 兑换码支付

### 业务流

```
用户（想买会员）
  ↓ 在闲鱼/微信/支付宝私下与卖家对话
  ↓ 转账 ¥15（Plus 月）或 ¥30（Pro 月）
卖家（你）
  ↓ 在后台 / 命令行生成兑换码
  ↓ 把兑换码发给买家
买家
  ↓ 打开墨阅 /user 页 → 输入兑换码 → 兑换
  ↓ 立即成为 Plus/Pro 会员
```

**资金路径**：买家 ↔ 卖家（走闲鱼/微信/支付宝直接转账），**不经过墨阅任何支付接口**。
**价值路径**：兑换码 → 墨阅服务端校验 → 写 `plans` 表 → 用户升级。

### 优势

- **0% 费率**（钱在买家卖家之间直接走，墨阅不抽）
- **0 开户费**（不需要第三方支付账户）
- **0 合规风险**（墨阅不参与资金流，仅是"凭证核验"）
- **0 第三方支付接口依赖**（代码完全自控，1-2 小时上线）

### 限制（提前说清楚）

- 闲鱼官方对"虚拟卡密"类目有监管，**大量售卖可能触发审核**。邀约制小规模 OK，正式大规模仍要 N09 合规
- 防泄漏/盗用靠"单次使用 + 期限 + 可选绑定用户"（见下）

---

## 三、技术设计

### 3.1 数据库（迁移 `025_redemption_codes.sql`）

```sql
-- 兑换码表
CREATE TABLE public.redemption_codes (
  code text PRIMARY KEY,                              -- Crockford 编码（4-4-4 带连字符）
  plan text NOT NULL CHECK (plan IN ('plus', 'pro')),
  months int NOT NULL DEFAULT 1 CHECK (months > 0),
  status text NOT NULL DEFAULT 'active'
    CHECK (status IN ('active', 'redeemed', 'expired', 'revoked')),
  redeemed_by uuid REFERENCES auth.users(id),
  redeemed_at timestamptz,
  expires_at timestamptz,                             -- 可选，到期不能兑换
  note text,                                          -- 卖家备注（如闲鱼订单号）
  created_by text NOT NULL,                           -- 管理员标识
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX idx_redemption_codes_status ON public.redemption_codes(status);
CREATE INDEX idx_redemption_codes_redeemed_by ON public.redemption_codes(redeemed_by);

-- RPC: 兑换（条件 UPDATE 防并发 + 续期 plans）
CREATE OR REPLACE FUNCTION public.redeem_code(
  p_code text,
  p_user_id uuid
) RETURNS json
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  v_code record;
  v_plan record;
  v_new_period_end timestamptz;
BEGIN
  -- 1. 锁住 code 行
  SELECT * INTO v_code
  FROM public.redemption_codes
  WHERE code = p_code
  FOR UPDATE;

  IF NOT FOUND THEN
    RETURN json_build_object('ok', false, 'reason', 'invalid_code');
  END IF;

  IF v_code.status != 'active' THEN
    RETURN json_build_object('ok', false, 'reason', 'already_used_or_invalid');
  END IF;

  IF v_code.expires_at IS NOT NULL AND v_code.expires_at < now() THEN
    UPDATE public.redemption_codes SET status = 'expired' WHERE code = p_code;
    RETURN json_build_object('ok', false, 'reason', 'expired');
  END IF;

  -- 2. 锁住 plans 行（防并发续期）
  SELECT * INTO v_plan FROM public.plans WHERE user_id = p_user_id FOR UPDATE;

  -- 3. 计算新到期时间（在当前基础上加 months；空表 / 已过期 → 从 now() 起算）
  IF v_plan.current_period_end IS NULL OR v_plan.current_period_end < now() THEN
    v_new_period_end := now() + (v_code.months || ' months')::interval;
  ELSE
    v_new_period_end := v_plan.current_period_end + (v_code.months || ' months')::interval;
  END IF;

  -- 4. 写 plans（upsert）
  INSERT INTO public.plans (user_id, plan, current_period_end, updated_at)
  VALUES (p_user_id, v_code.plan, v_new_period_end, now())
  ON CONFLICT (user_id) DO UPDATE SET
    plan = EXCLUDED.plan,
    current_period_end = EXCLUDED.current_period_end,
    updated_at = now();

  -- 5. 标记 code 已用
  UPDATE public.redemption_codes
  SET status = 'redeemed', redeemed_by = p_user_id, redeemed_at = now()
  WHERE code = p_code;

  RETURN json_build_object(
    'ok', true,
    'plan', v_code.plan,
    'months_added', v_code.months,
    'current_period_end', v_new_period_end
  );
END;
$$;

-- RPC: 管理员生成兑换码
CREATE OR REPLACE FUNCTION public.create_redemption_codes(
  p_count int,
  p_plan text,
  p_months int DEFAULT 1,
  p_expires_in_days int DEFAULT NULL,
  p_note text DEFAULT NULL,
  p_created_by text DEFAULT 'admin'
) RETURNS json
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  v_codes text[] := ARRAY[]::text[];
  v_code text;
  v_i int;
BEGIN
  IF p_count < 1 OR p_count > 100 THEN
    RETURN json_build_object('ok', false, 'reason', 'invalid_count');
  END IF;

  IF p_plan NOT IN ('plus', 'pro') THEN
    RETURN json_build_object('ok', false, 'reason', 'invalid_plan');
  END IF;

  FOR v_i IN 1..p_count LOOP
    -- 复用 src/utils/invite.ts 的 Crockford 生成逻辑（不直接复用 TS，在 SQL 里用 random 拼）
    v_code := upper(
      encode(gen_random_bytes(8), 'hex')
    ); -- TODO: 用 PL/pgSQL 写 Crockford 生成；或上层 TS 生成后 INSERT
    -- 此处用 hex 占位，实际实现时改成 Crockford 4-4-4
    INSERT INTO public.redemption_codes (code, plan, months, expires_at, note, created_by)
    VALUES (
      v_code,
      p_plan,
      p_months,
      CASE WHEN p_expires_in_days IS NOT NULL
        THEN now() + (p_expires_in_days || ' days')::interval
        ELSE NULL END,
      p_note,
      p_created_by
    );
    v_codes := array_append(v_codes, v_code);
  END LOOP;

  RETURN json_build_object('ok', true, 'codes', to_json(v_codes));
END;
$$;

-- 全部 RPC 加权限
REVOKE EXECUTE ON FUNCTION public.redeem_code FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.redeem_code TO service_role;

REVOKE EXECUTE ON FUNCTION public.create_redemption_codes FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION public.create_redemption_codes TO service_role;
```

> **简化方案**：管理员 RPC 由 TS 层生成 Crockford 码然后 INSERT，**不在 SQL 里拼码**（减少 PL/pgSQL 复杂度 + 复用 `src/utils/invite.ts` 已有的 Crockford 工具）。

**最终方案**：只保留 `redeem_code` RPC，`create_redemption_codes` 由 TS 在 `src/utils/redemption.ts` + `src/app/api/admin/redemption-codes/route.ts` 里实现。

### 3.2 服务端路由

| 路径 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/api/redeem` | POST | 用户 JWT | 用户输入兑换码 → 调 `redeem_code` RPC |
| `/api/admin/redemption-codes` | GET | 管理员 JWT | 列出兑换码（带 redemptions 聚合） |
| `/api/admin/redemption-codes` | POST | 管理员 JWT | 生成 N 个兑换码 |

**`/api/redeem` 实现**：

```typescript
// src/app/api/redeem/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getUserIdFromRequest } from '@/utils/auth';
import { createSupabaseAdmin } from '@/utils/supabase';
import { normalizeInviteCode } from '@/utils/invite'; // 复用 Crockford 规范化

export async function POST(req: NextRequest) {
  const userId = await getUserIdFromRequest(req);
  if (!userId) return NextResponse.json({ error: 'unauthorized' }, { status: 401 });

  const { code } = await req.json();
  if (typeof code !== 'string' || !code.trim()) {
    return NextResponse.json({ error: 'invalid_request' }, { status: 400 });
  }

  // Crockford 规范化：去空格/连字符/全角 → uppercase
  const normalized = code.replace(/[\s\u3000-]/g, '').toUpperCase();
  if (normalized.length < 8) {
    return NextResponse.json({ error: 'invalid_code' }, { status: 400 });
  }

  const supabase = createSupabaseAdmin();
  const { data, error } = await supabase.rpc('redeem_code', {
    p_code: normalized,
    p_user_id: userId,
  });

  if (error) {
    return NextResponse.json({ error: 'redeem_failed' }, { status: 500 });
  }

  if (!data.ok) {
    // 错误码 i18n 映射在客户端做
    return NextResponse.json({ error: data.reason }, { status: 400 });
  }

  return NextResponse.json({
    ok: true,
    plan: data.plan,
    monthsAdded: data.months_added,
    currentPeriodEnd: data.current_period_end,
  });
}
```

**`/api/admin/redemption-codes` 实现**：

```typescript
// src/app/api/admin/redemption-codes/route.ts
// 仿照 /api/auth/invite/admin 的 ADMIN_EMAILS 门禁
// GET: 列 codes + 关联 user (redeemed_by email)
// POST: { plan, months, count, expiresInDays?, note? } → 生成 → 返回 codes 列表
```

### 3.3 客户端 UI

**入口位置**：`/user` 页（订阅管理页）加新卡片"兑换码"或新 tab。

**UI 设计**（最小可用）：

```
┌──────────────────────────────────────┐
│  兑换会员码                          │
│                                      │
│  请输入您从卖家获取的兑换码：        │
│  ┌──────────────────────────────┐    │
│  │ XXXX-XXXX-XXXX               │    │
│  └──────────────────────────────┘    │
│  [    兑换    ]                      │
│                                      │
│  兑换成功后立即生效，会员时长叠加     │
└──────────────────────────────────────┘
```

**错误码 i18n 映射**（zh-CN/zh-TW 各 +5 键）：
- `invalid_code` → "兑换码无效"
- `already_used_or_invalid` → "兑换码已被使用或失效"
- `expired` → "兑换码已过期"
- `redeem_failed` → "兑换失败，请稍后重试"
- `success` → "兑换成功！{months} 个月 {plan} 会员已激活"

**不影响**：现有 onSubscribe 三分发（IAP / epay / stripe）+ `MEMBERSHIP_PLANS` 常量 + `/user` 页其他部分。

### 3.4 与现有 epay 代码的关系

- **保留** `src/libs/payment/epay.ts` + 4 路由 + 客户端 epay 流程（已 commit `d935ef2`）
- **理由**：未来商业化转 YunGouOS 时可复用 epay 框架；现在禁用但保留代码
- **客户端层面**：`/user` 页 onSubscribe 暂时屏蔽 epay 路径（`PAYMENT_PROVIDER=epay` 但 gateway 空时本就是 503，等于自动屏蔽）
- **或者彻底删除 epay**（N08 实施时决定）

---

## 四、用户/管理员操作流

### 4.1 卖家（你）生成兑换码

两种方式任选：

**方式 A · API**（推荐，自动化）：
```bash
# 登录拿管理员 token
TOKEN=$(curl -s -X POST "http://localhost:8000/auth/v1/token?grant_type=password" \
  -H "apikey: $ANON" -H "Content-Type: application/json" \
  -d '{"email":"admin@chen-cn.top","password":"<pwd>"}' | jq -r .access_token)

# 生成 5 个 Plus 月卡
curl -X POST http://localhost:3000/api/admin/redemption-codes \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"plan":"plus","months":1,"count":5,"expiresInDays":365,"note":"闲鱼-2026-09-07-张三"}'
```

**方式 B · SQL 直插**（最直接，不写代码）：
```bash
cd ~/readest/docker
sudo docker compose exec -T db psql -U supabase_admin -d postgres -c "
  INSERT INTO public.redemption_codes (code, plan, months, expires_at, note, created_by)
  VALUES
    ('MOYU-AAAA-BBBB', 'plus', 1, now() + interval '365 days', '闲鱼-张三', 'admin'),
    ('MOYU-CCCC-DDDD', 'plus', 1, now() + interval '365 days', '闲鱼-张三', 'admin');"
```

> 码格式：Crockford 字母表 `0-9 A-H J K M N P-T V-Z`（去 I/L/O/U），4-4-4 带连字符 —— 与 `src/utils/invite.ts` 邀请码同字符表，方便记忆。

### 4.2 买家兑换

1. 打开 `https://readest.chen-cn.top/user`
2. 滚到"兑换码"卡片
3. 输入卖家给的码（自动规范化大小写/连字符）
4. 点"兑换"
5. 成功后 toast 提示，立即生效
6. `/api/user/plan` 应返回 `plan:plus, quota:2GB`

### 4.3 卖家后续动作

- 查兑换记录：`/api/admin/redemption-codes` GET 看 `redeemed_at` + 关联 user email
- 撤销异常码（漏发/被截）：SQL 改 `status='revoked'`

---

## 五、节点拆分

| 子节点 | 范围 | 状态 |
|---|---|---|
| N08a | DB 迁移 025（redemption_codes 表 + redeem_code RPC） | ⬜ |
| N08b | 服务端 `/api/redeem` + `/api/admin/redemption-codes` 路由 + 单测 | ⬜ |
| N08c | 客户端 UI（`/user` 页兑换码卡片 + i18n 5 键） | ⬜ |
| N08d | Crockford 工具复用 + 管理端鉴权（仿照 invite/admin） | ⬜ |
| N08e | 端到端验证（生成码 → 兑换 → plan 升级） | ⬜ |
| N08f | 部署（rsync 源码 + 跑迁移 + 重打镜像 + up -d） | ⬜ |
| N08g | 测试基线 44 不变 + commit 落地 | ⬜ |

**全部子节点都无外部依赖**（不阻塞）。

---

## 六、验证基线

- **DB**：迁移 025 应用成功（`readest_meta.migrations` 含 `025_redemption_codes.sql`）
- **服务端**：`POST /api/redeem` 端到端：未登录 → 401；非法码 → 400 invalid_code；过期码 → 400 expired；已用码 → 400 already_used_or_invalid；正常码 → 200 ok=true
- **管理员**：`POST /api/admin/redemption-codes` 生成 N 个码 → GET 列表看到 → 任一码能正常兑换
- **客户端**：`/user` 页输入码 → 兑换成功 → 配额 / 档位立即刷新
- **测试**：`pnpm test` 失败数仍 = 44（基线不变）；新加 redeem 测试文件（如有）
- **lint**：`pnpm lint` 0 issues
- **commit**：分批提交（schema / 路由 / 客户端 / 部署 各成 commit）

---

## 七、风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 兑换码泄漏（卖家被截胡、买家二次传播） | 中 | 单次使用 + 可选 `expires_at`；管理员 GET 列表审计 |
| 闲鱼审核虚拟卡密类目 | 中 | 小规模测试 OK；月销超过 50 单触发 N09 合规 |
| 兑换码格式不规范（用户输入错误） | 低 | Crockford 规范化 + 长度校验 + 错误码明确 |
| 兑换码与邀请码字符表冲突 | 低 | 同字符表但前缀区分（兑换码用 `MOYU-` 起头，邀请码无前缀） |
| RPC 注入 / 并发漏洞 | 低 | `SECURITY DEFINER` + 全部 `REVOKE` 公共 + `FOR UPDATE` 行锁 |

---

## 八、不在本节点范围

- ❌ 第三方支付接口（YunGouOS / 虎皮椒）—— 暂不接
- ❌ 自动续费（订阅）—— 兑换码一次性
- ❌ 退款流程（兑换码视为"已发货虚拟商品"，不退）—— 在闲鱼对话里卖家手动决定
- ❌ 兑换码转让/转赠功能（一次绑定一个用户）—— 暂不需要
- ❌ N09 合规（个体户 / APP 备案）—— 规模扩大再做

---

## 九、相关改动列表

- **新增** `docker/volumes/db/migrations/025_redemption_codes.sql`
- **新增** `src/app/api/redeem/route.ts`
- **新增** `src/app/api/admin/redemption-codes/route.ts`
- **新增** `src/utils/redemption.ts`（Crockford 兑换码生成/规范化 + 错误码枚举）
- **新增** `src/__tests__/utils/redemption.test.ts`
- **新增** `src/__tests__/api/redeem.test.ts`（如有 mock 框架）
- **改** `src/app/user/page.tsx`（加"兑换码"卡片）
- **改** `public/locales/{zh-CN,zh-TW}/translation.json`（各 +5 键）
- **改** `src/services/constants.ts`（如需新增 `REDEMPTION_ERROR_MESSAGES` 常量）
- **不改**：`墨阅项目规则.md` 第四节（产品规格 + epay 描述保留作为"未来路径"）
- **不动**：`src/libs/payment/epay.ts` + 4 路由（保留作为未来扩展）
