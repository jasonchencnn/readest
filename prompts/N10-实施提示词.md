# N10 实施提示词 · 内置合规章扩展 + 适配器层

> 复制下面"提示词正文"整段，粘贴到新会话作为首条消息。

---

## 提示词正文

```
你是墨阅项目 v0.2.0 节点 N10 的实施者（worker 角色）。本会话唯一任务 = 按 ./需求-v0.2.0-书源整合.md 第五章 N10 节实施。

## 一、必读（按顺序读，不读不许动手）

1. `项目主控.md` 第 4 节"当前节点" — 确认当前里程碑 = v0.2.0，当前节点 = N10
2. `项目主控.md` 第 2 节"v0.2.0 节点表" — N10 行（含依赖 / 交付物 / 验证基线）
3. `项目主控.md` 第 6 节 ADR-2026-09-09-01~04（v0.2.0 启动 / Legado JSON / UI 推 v0.3.0 / 合规边界）— 不要越界做 N11/N12/N14/N15
4. `墨阅项目规则.md` 第四节 4.5.1"内置合规章清单"（v0.2.0 起持续维护）— 已有 4 个复用 + 4 个候选新增
5. `墨阅项目规则.md` 第五节"合规红线"全 5 条 — 任何 commit 前 grep 检查
6. `需求-v0.2.0-书源整合.md` 第五章 N10 节 — 详细设计（候选源 / 适配器架构 / 工程量 / 验证 / 风险）

## 二、任务范围（IN-SCOPE）

**只做 N10，N11/N12/N14/N15 不动**（即使发现顺手能做的事）。

### N10 交付物
- `apps/readest-app/src/app/opds/components/CatalogManager.tsx` 加 5-8 个合规章（4 复用 + 1-4 实测通过的新增）
- 新增适配器层：`apps/readest-app/src/app/opds/adapters/`（`opds` / `mediawiki` / `archive-advancedsearch` / `custom-http` 4 类型）
  - 适配器签名：`interface SourceAdapter { type: ...; search(query, page?): Promise<SearchResult[]>; getBook(id): Promise<BookDetail>; }`
  - 公共类型放 `apps/readest-app/src/app/opds/adapters/types.ts`
- i18n 键：zh-CN / zh-TW / en 各加 4-8 键（每个新源名称 + 描述）
- 测试 fixture：每个新源 1 个 fixture（mock 适配器返回）
- 适配器层单测 ≥ 4

### N10 验证基线
- `pnpm lint` 0 issues
- `pnpm test` 全量 **44 failed / 9515+ passed** = 预存基线无回归（**新失败 = 回归，必须立刻修**）
- CatalogManager.tsx 含 5-8 个源
- 每个新源 URL 200（curl 验证）+ 内容明确公版/CC/授权
- 至少 1 个中文源（用户硬要求，优先级 #1 = `wikisource-zh`）

## 三、必跑流程（按顺序）

1. **实测 4 个候选新增源**（用 curl + UA + 抽样内容确认公版）：
   - `https://zh.wikisource.org/w/api.php?action=opensearch&format=json` — 中文维基文库（**必入**）
   - `https://en.wikisource.org/w/api.php?action=opensearch&format=json` — 英文维基文库
   - `https://archive.org/advancedsearch.php?q=mediatype%3A%28texts%29+AND+licenseurl%3A%28%2Apublicdomain%2A%29&output=json&rows=5` — IA 公版
   - `https://www.doabooks.org/` — DOAB（带 UA 重试）
2. **写适配器层**（`opds` / `mediawiki` / `archive-advancedsearch` / `custom-http`）
3. **改 CatalogManager.tsx**：把 4 个复用 + 实测通过的新增源加进 `POPULAR_CATALOGS` 数组；每个新源带 `adapterType` 字段标识走哪个适配器
4. **加 i18n 键**：`public/locales/{zh-CN,zh-TW,en}/translation.json` 各加 4-8 键
5. **加测试 fixture**：`src/__tests__/app/opds/adapters/*.test.ts`（每类型 1 个）+ 改 `src/__tests__/app/opds/opds-utils.test.ts` 适配新结构
6. **跑基线**：`pnpm lint` + `pnpm test` 确认 44 失败不变
7. **commit**：`feat(opds): expand built-in catalogs with adapters layer (N10)`（**不带** `--no-verify` 因为基线 44 是预存不需绕过）
8. **更新 主控文档**（必做）：第 2 节 N10 行 ✅ + 完成时间 + commit hash / 第 4 节当前节点切到 N11a / 第 8 节更新记录追加

## 四、不做的事（HARD CONSTRAINTS）

- ❌ **不接** ZLibrary / Anna's Archive / Library Genesis / Libgen / 笔趣阁 等任何盗版/侵权源
- ❌ **不接** Legado 社区书源市场 / 不提供"导入 Legado 分享源"入口 / 不支持 `legado://import/` 协议
- ❌ **不做**内嵌浏览器 / 任意 URL 渲染
- ❌ **不做** UI 提级（"在线书库"顶级 Tab / 源管理 UI / 健康度徽章）— 推 v0.3.0
- ❌ **不实施** N11 / N12 / N14 / N15（即使发现顺手能改的）
- ❌ **不缓存**书内容到墨阅服务器（仅代理请求 + 元数据缓存）
- ❌ **不引入** `vm2` / `isolated-vm`（沙箱选型留给 N11b，详见 ADR-2026-09-09-06）
- ❌ **不修改** readest 上游 4 个复用源的 `id` / `url`（破坏用户已配）
- ❌ **不修改** `apps/readest-app/src/app/opds/components/CatalogManager.tsx` 已有 4 个源的 `id` 命名（与 readest 上游完全一致）
- ❌ **不跳** `pnpm test`（任何新失败 = 回归，必须立刻修）
- ❌ **不**为绕过 44 failed 用 `--no-verify`（那是 husky 误报问题，N07 标记的"待修"事项，不在 N10 范围）

## 五、合规 grep 检查（commit 前必跑）

```bash
# 应该 0 命中
grep -rE "zlibrary|libgen|anna-archive|biququ|笔趣阁" apps/readest-app/src/ 2>/dev/null
grep -rE "legado://" apps/readest-app/src/ 2>/dev/null
grep -rE "vm2|isolated-vm" apps/readest-app/src/ 2>/dev/null
```

任意命中 1 条 = 立刻停下 + 报告用户。

## 六、报告格式（完结时必给）

按下面 6 段输出（不要散装）：

```
[N10 实施完成报告]

1. 实测源清单（URL / 状态 / 公版依据 / 接入适配器类型）
2. 新增适配器文件路径
3. CatalogManager.tsx 变更摘要（行数 / 新增源 ID / adapterType 映射）
4. i18n 键清单（zh-CN / zh-TW / en 各多少键）
5. 验证结果（pnpm lint 0 issues + pnpm test 失败数 = 44 不变）
6. Commit hash + 主控文档同步状态

残留 / 风险 / 用户需做的 1-2 件事（如有）
```

## 七、做完立即停下

不要顺手做 N11a（即使发现 N11a 接口刚好需要先调整）。N10 完结 → 报告 → 等用户启动 N11a 实施会话。

---

## 提示词正文结束
```

---

## 使用方式

1. 复制"提示词正文"整段（含 ``` 围栏）
2. 粘贴到新会话作为**首条消息**
3. 实施者按"必读 / 任务范围 / 必跑流程 / 不做的事 / 合规 grep / 报告格式"7 段执行
4. 完结后给我回 [N10 实施完成报告]，我接 N11a
