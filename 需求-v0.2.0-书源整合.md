# 需求-v0.2.0 · 墨阅书源整合

> **本文档是 v0.2.0 的需求与设计文档**。节点 N10–N15 的实施以本文档为唯一真相源。
> 关联：`./项目主控.md`（节点表 + 决策日志 + 更新记录）/ `./墨阅项目规则.md`（品牌/规格/合规）/ `./需求-N08-兑换码支付.md`（v0.1.0 阶段 4 设计参考）。
> **审批状态**：✅ 2026-09-09 15:48 用户审批通过。
> **当前状态**：🟡 启动中（等用户启动实施会话）。

---

## 一、背景

### 1.1 现状（v0.1.0 收官）
- 墨阅已上线 `https://readest.chen-cn.top`，N00–N08 全部完成（除 N04/N06/N09 外部依赖）。
- "在线书源" = 复用 readest 上游内置 4 个 OPDS 源（Project Gutenberg / Standard Ebooks / ManyBooks / Unglue.it）。
- 4 个源**全英文**、**全海外**、**全是英文公版** —— 中文公版源 0 个。

### 1.2 用户痛点
- 中文公版书无内置源
- 没有"自定义书源格式"（readest 只支持 OPDS URL 接入）
- 没有"跨源聚合搜索"（用户得自己切 4 个源各搜一遍）
- UI 仍是 readest 风格，未做品牌分流

### 1.3 已否决的方案
- ❌ ZLibrary 接入（CLI / 适配器 / 浏览器内嵌）— push back 3 次，理由不变
- ❌ Legado 社区书源市场 / 一键导入 Legado 分享源 — 90% 盗版
- ❌ 内嵌浏览器访问 zlibrary — 举证责任倒置

### 1.4 决策路线
**"收格式不收生态"** — 自定义书源 = **Legado JSON 规范子集**（成熟规范 / 0 迁移成本 / 不接入 Legado 社区源）。
**新增**：社区书源分享（N15）—— 用户可上传/引用/举报，**有审核 + 黑名单 + 24h 下架**作为合规护城河。

---

## 二、目标

v0.2.0 = "墨阅书源 v1" = 让墨阅拥有**可治理、可扩展、可合规**的书源生态。

**三件事**：
1. **N10** 内置 5-8 个合规章（4 复用 + **至少 1 中文** + 适配器层）
2. **N11** 自定义书源 = Legado JSON 子集 + **JS 沙箱增强**
3. **N12** 跨源聚合搜索
4. **N15** 社区书源分享（用户上传 + 展示 + 引用 + 举报）

**不做**（推到 v0.3.0）：UI 提级（"在线书库"顶级 Tab + 源管理 UI + 健康度徽章）。

---

## 三、范围

### In-Scope（v0.2.0 必做）
- N10 内置合规章扩展（4 复用 + 1-4 新增，含**至少 1 中文**）+ 适配器层抽象
- N11 自定义书源 = Legado JSON 子集（XPath / JSONPath / CSS / **JS 沙箱**）+ 源管理 UI
- N12 跨源聚合搜索（fan-out + 合并去重 + 中文优先）
- N14 合规与审核基础设施（举报 + 黑名单 + 免责声明）
- N15 社区书源分享（迁移 026 + 上传 + 展示 + 引用 + 举报 + 审核队列）
- `墨阅项目规则.md` 同步新增"内置合规章清单"和"N15 合规红线"
- 决策日志追加 6 条 ADR

### Out-of-Scope（v0.2.0 不做）
- ❌ ZLibrary / Anna's Archive / Library Genesis 任何形式接入
- ❌ Legado 社区书源市场 / 一键导入 Legado 分享源
- ❌ UI 提级（推到 v0.3.0）
- ❌ 服务端缓存书内容到墨阅（仅代理请求 + 元数据缓存）
- ❌ 集成 Legado app 引擎（架构不匹配，跨 app 集成复杂度高）
- ❌ 内嵌浏览器 / 任意 URL 渲染

---

## 四、关键决策（6 条 ADR）

### ADR-2026-09-09-01 · v0.2.0 启动
- **背景**：v0.1.0 收官后用户提出"书源整合"方向
- **决策**：v0.2.0 定位"墨阅书源 v1"，节点 N10–N15
- **后果**：UI 推 v0.3.0；总工期 ~18-25 天

### ADR-2026-09-09-02 · 自定义书源格式 = Legado JSON 子集
- **背景**：readest 只支持 OPDS URL；墨阅需要支持非 OPDS 站点
- **决策**：墨阅自定义书源格式 = **Legado JSON 规范的子集**（`bookSourceName` / `searchUrl` / `ruleSearch` / `ruleBookName` / `ruleChapterList` / `ruleBookContent` / `ruleToc` / JS 增强等）
- **后果**：
  - 懂 Legado 的用户 0 迁移成本
  - 借用 Legado 文档生态（合规版）
  - 引擎复用现成思路（XPath / JSONPath / JS 沙箱）
- **不接 Legado 生态**：墨阅不预置 Legado 社区源、不提供"导入 Legado 分享源"入口

### ADR-2026-09-09-03 · UI 提级推 v0.3.0
- **背景**：原计划"在线书库"提为顶级 Tab + 源管理 UI
- **决策**：v0.2.0 保留 readest `/opds` 入口；UI 提级推 v0.3.0
- **后果**：v0.2.0 优先把"书源能力"做扎实，UI 是锦上添花

### ADR-2026-09-09-04 · 合规边界再确认
- **不接**：ZLibrary / Anna's Archive / Library Genesis / Legado 社区源 / 浏览器内嵌
- **可接**：Legado JSON 格式（用户自写合规源）+ 公版 OPDS 源 + 自定义 OPDS URL
- **服务端**：只代理请求 + 元数据缓存，**不缓存**书内容到墨阅

### ADR-2026-09-09-05 · N15 社区书源分享上线
- **背景**：原方案"❌ 不做用户共建书源市场"，但用户决策"做社区分享，要保留上传源的展示、引用功能"
- **决策**：新增 N15 节点，实现社区书源分享
- **合规边界**（**N15 红线**）：
  1. 平台**不预置**任何用户上传源到内置清单
  2. 平台**不展示**源指向的具体内容（书 / 封面 / 摘要）— **只展示源元数据**
  3. 平台**不排行**（不按引用数排序，避免引导向热门源）
  4. 平台有**审核 + 举报 + 黑名单 + 24h 下架**机制
  5. 平台有**免责声明**："墨阅社区书源由用户上传，平台不为其内容负责"
- **后果**：
  - 迁移 026（`moyue_user_sources` / `moyue_source_reports` / `moyue_source_blacklist` 三表 + RLS + 审核 RPC）
  - 管理员端审核队列
  - 用户端上传 / 展示 / 引用 / 举报 UI
  - 工程量 ~5-6 天

### ADR-2026-09-09-06 · JS 沙箱选型 quickjs-emscripten
- **背景**：N11 用户决策"带 JS 增强"，需选沙箱
- **决策**：采用 **`quickjs-emscripten`**（嵌入式 QuickJS via WebAssembly）
- **理由**：
  - 沙箱级别最高（独立运行时，非 Node 子进程）
  - 跨平台一致（Mac / Windows / Linux / Android / iOS / Web 行为一致）
  - 性能可接受（小型书源规则 JS 脚本无压力）
  - 避免 `isolated-vm` 在 macOS / Windows 上的 native binding 编译问题（需 node-gyp + python + c++ 工具链）
- **沙箱边界**：
  - 超时 5s / 内存 64MB
  - 禁用：`require` / `process` / `globalThis.fetch` / `fs` / `net` / 动态 `eval` / `Function` 构造
  - 白名单 helper：`httpGet(url, headers)` / `httpPost(url, body, headers)` / `log(...)` / `base64Encode/decode` / `parseHtml(html)` / `crypto.md5/sha256`
- **静态校验**：服务端 schema 校验上传 JSON，扫描危险模式（`require` / `process` / `eval` 字面量）拒绝
- **包大小影响**：~500KB wasm
- **废弃**：`vm2`（2023-2024 多起 RCE，已弃维）

---

## 五、节点拆分

### N10 · 内置合规章扩展

**目标**：CatalogManager.tsx 加 5-8 个合规章

**复用**（readest 上游已有 4 个，**已知 OK**）：
- `gutenberg` · Project Gutenberg — `https://m.gutenberg.org/ebooks.opds/`
- `standardebooks` · Standard Ebooks — `https://standardebooks.org/feeds/opds`
- `manybooks` · ManyBooks — `https://manybooks.net/opds/index.php`
- `unglue.it` · Unglue.it — `https://unglue.it/api/opds/`

**新增候选**（优先级排序）：
1. **`wikisource-zh`** · 中文维基文库（公版古籍：楚辞 / 诗经 / 唐诗 / 论语等）— **必入**（用户决策"增加中文源"）
   - URL：`https://zh.wikisource.org/w/api.php`
   - 协议：MediaWiki API（非 OPDS）— 需适配器
   - 内容：明确公版（CC-BY-SA 3.0 / 公共领域）
2. `wikisource-en` · 英文维基文库 — MediaWiki API 适配器
3. `internet-archive-oa` · Internet Archive 公共领域区 — `https://archive.org/advancedsearch.php`（非 OPDS）
4. `doab` · DOAB（学术专著 CC，英文为主）— 待测 UA
5. 候选中文公版：国学数典（论坛形式，无 OPDS）/ 国家图书馆公版（PDF 扫描）/ 浙江公版（PDF 扫描）— **多数不达标**（非 EPUB/可读文本）

**适配器架构**（为 N11 铺路）：
- 不在 CatalogManager.tsx 硬编码所有源，新增源走统一适配器层
- 适配器类型：`opds`（默认） / `mediawiki` / `archive-advancedsearch` / `custom-http`
- 适配器签名：
  ```typescript
  interface SourceAdapter {
    type: 'opds' | 'mediawiki' | 'archive-advancedsearch' | 'custom-http';
    search(query: string, page?: number): Promise<SearchResult[]>;
    getBook(bookId: string): Promise<BookDetail>;
  }
  ```

**新增要求**：
- 每个新源**必须实测**：URL 200 + 内容明确公版/CC/授权
- 写入 `墨阅项目规则.md` 第四节"内置合规章清单"
- 含完整 i18n 键（zh-CN / zh-TW / en）
- 1 个 fixture 测试

**工程量**：~3-4 天（中文源适配器占 1 天）

**验证**：
- CatalogManager.tsx 含 5-8 个源
- `pnpm test` 44 失败不变
- 适配器层单测 ≥ 4 个

**风险**：
- 中文公版源适配器复杂度高（MediaWiki API 协议非标准）
- **缓解**：先把海外 2 个 + 中文 1 个跑通，剩余推到 v0.2.1

---

### N11 · 自定义书源（Legado JSON 子集 + JS 沙箱）

**目标**：墨阅支持导入/编辑/管理自定义书源

#### N11a · 基础解析

**JSON Schema 定义**（墨阅书源 = Legado 子集 + 墨阅扩展）：
```json
{
  "bookSourceName": "示例书源",
  "bookSourceUrl": "https://example.com",
  "bookSourceType": 0,                  // 0: 文本 / 1: 音频 / 2: 图片
  "bookSourceGroup": "公版;中文",
  "enabled": true,
  "searchUrl": "https://example.com/search?q={{key}}&page={{page}}",
  "ruleSearch": {
    "bookList": "//div[@class='book-item']",
    "name": "h3/text()",
    "author": "p[@class='author']/text()",
    "coverUrl": "img/@src",
    "bookUrl": "a/@href",
    "intro": "p[@class='desc']/text()",
    "kind": "span[@class='tag']/text()",
    "lastChapter": "p[@class='latest']/text()"
  },
  "ruleBookInfo": { /* 同 ruleSearch 结构 */ },
  "ruleToc": {
    "chapterList": "//div[@class='chapters']/a",
    "chapterName": "text()",
    "chapterUrl": "@href"
  },
  "ruleContent": {
    "content": "//div[@id='content']/text()",
    "nextContentUrl": "//a[@class='next']/@href"
  },
  "customOrder": 0,
  "enabledExplore": true,
  "enabledCookieJar": false
}
```

**解析引擎**：
- **XPath**（HTML 解析用 `node-html-parser` + `xpath`）— 复用 Legado 思路
- **JSONPath**（`jsonpath-plus`）— 处理 API 返回 JSON
- **CSS 选择器**（`cheerio`）— 简化规则
- **占位符替换**：`{{key}}` / `{{page}}` / `{{baseUrl}}` 在请求 URL 中替换

**源管理 UI**（最小版，`/opds` 页加"自定义源" Tab）：
- 列表（名称 / URL / 类型 / 启用 / 上次测试时间）
- 新建（表单 + JSON 编辑器切换）
- 编辑（同步新建）
- 导入/导出 JSON 文件
- 删除
- **不做**（推 v0.3.0）：健康度徽章 / 批量操作 / 源市场集成

**测试**：
- 解析器单测（5+ fixture）
- 端到端：1 个真实合规源走通搜→看

**工程量**：~5 天

#### N11b · JS 沙箱增强

**沙箱引擎**：`quickjs-emscripten`（详见 ADR-2026-09-09-06）

**沙箱边界**：
- 超时 5s / 内存 64MB
- 禁用：`require` / `process` / `globalThis.fetch` / `fs` / `net` / 动态 `eval` / `Function` 构造
- 白名单 helper：
  - `httpGet(url, headers)` / `httpPost(url, body, headers)` — 走服务端代理（统一 UA + 限速）
  - `log(...)` / `base64Encode/decode` / `parseHtml(html)` / `crypto.md5/sha256`

**JS 增强使用场景**：
- `searchUrl` 动态生成（带签名 / 带时间戳）
- `ruleBookContent` 后处理（去除广告 / 合并段落）
- 翻页逻辑（自定义 next page 解析）

**静态校验**（服务端）：
- 上传 JSON 时扫描危险模式（`require` / `process` / `eval` / `Function` 字面量）
- 不通过则 400 拒绝，提示修改

**测试**：
- 沙箱安全测试（防 escape）
- 性能测试（5 并发源 × 10s 持续 = 无 OOM）
- 端到端：1 个 JS 增强源走通

**工程量**：~4-7 天

**N11 总工程量**：~9-12 天

**关键决策点**：
- N11a 必须完成才能启动 N11b
- N11b 失败回退：仅支持 XPath + JSONPath + CSS（能力损失可接受）

---

### N12 · 跨源聚合搜索

**目标**：一次输入 → 并行 N 源搜索 → 合并去重 → 统一列表

**架构**：
```
[Client 搜索框]
   ↓
[Server /api/search/opds?q=]
   ↓ (fan-out, 并行 N 个源)
[代理层: User-Agent + 限速 + 元数据缓存 (TTL 24h)]
   ↓ (合并去重)
[结果: { hits: [...], sources: [...], totalTime }]
```

**关键设计**：
- 单源超时 5s，整体超时 15s
- 命中合并：title + author 相似度（Levenshtein 距离 ≤ 3 = 同一书）
- 排序：中文源优先 + 命中数加权 + 新书加权
- 服务端**只代理请求**，**不缓存书内容**到墨阅（避免变成"主动接入"）
- 可缓存**元数据**（书名 / 作者 / 封面 URL），TTL 24h
- 失败源优雅降级（不阻塞整体，超时标灰）

**API 契约**：
```typescript
// GET /api/search/opds?q=三体&page=1
// Response
interface SearchResponse {
  hits: SearchHit[];
  sources: { id: string; name: string; status: 'ok' | 'timeout' | 'error'; hitCount: number; timeMs: number }[];
  totalTime: number;
  page: number;
  hasMore: boolean;
}
interface SearchHit {
  id: string;                  // 源内 ID
  sourceId: string;            // 哪个源
  title: string;
  author: string;
  coverUrl?: string;
  intro?: string;
  tags?: string[];
  score: number;               // 排序分
}
```

**工程量**：~3-4 天

**验证**：
- 5 个内置源同时搜同一本书名 → 1.5s 内返回合并结果
- 慢源/失效源不阻塞整体
- 中文优先排序生效

**风险**：
- 服务端被反爬 → User-Agent + 限速 + 元数据缓存
- 跨源合并去重精度 → 引入 ISBN 优先匹配（如果命中）

---

### N14 · 合规与审核基础设施

**目标**：自定义书源有治理机制，不开放"任意 URL 一键接入"

**范围**：
1. **管理员端**：`/admin/sources` 审核队列
   - 待审 / 通过 / 拒绝 / 拒绝原因
   - 启用 / 禁用 / 1 键下架
   - 审计日志
2. **服务端黑名单**：
   - `moyue_source_blacklist` 表（域名 / URL 模式 + 原因 + 创建时间）
   - 上传时先扫黑名单
   - 用户举报 → 审核 → 加入黑名单
3. **用户协议补充**：
   - "墨阅不为其接入的书源内容负责"
   - "违规书源由用户举报后下架"
4. **内置源清单**：写入 `墨阅项目规则.md` 第四节作为合规护城河
5. **运营 SOP**：
   - 24h 内响应举报
   - 每周审计一次"被举报"源
   - 月度审计一次"低引用数但启用"源（潜在违规）

**工程量**：~2-3 天（与 N10-N15 同步）

**验证**：
- 举报 → 审核 → 下架流程走通
- 内置源清单 grep 0 命中侵权源
- 黑名单上传时拦截

---

### N15 · 社区书源分享

**目标**：用户上传源 → 审核通过 → 展示 + 引用 + 举报

**核心功能**：
- **上传**：登录用户可上传 JSON 源（复用 N11a 的 schema 校验 + JS 沙箱扫描）
- **审核**：待审 → 人工 + 自动扫描 → 通过/拒绝（带原因）
- **展示**：源卡片网格（书源名 / URL / 描述 / 标签 / 上传者 / 引用数 / 上传时间）
  - **不展示**源指向的内容（书 / 封面 / 摘要）— **只展示源元数据**
- **引用**：一键"引用"到自己的源列表（自动跟随源更新，源被下架时引用自动失效）
- **搜索 / 标签筛选**（"公版 / CC / 中文 / 学术 / 文学 / 历史"等）
- **举报**：每个源卡片有举报按钮 → 管理员处理

**数据模型**（迁移 026）：

```sql
-- 社区书源（用户上传）
CREATE TABLE moyue_user_sources (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users NOT NULL,
  name text NOT NULL,
  url text NOT NULL,
  description text,
  tags text[] DEFAULT '{}',
  source_json jsonb NOT NULL,        -- Legado JSON 格式 + 沙箱安全扫描结果
  status text CHECK (status IN ('pending', 'approved', 'rejected', 'taken_down')) DEFAULT 'pending',
  reject_reason text,
  reviewed_by uuid REFERENCES auth.users,
  reviewed_at timestamptz,
  cite_count int DEFAULT 0,           -- 引用数（不参与搜索排名）
  created_at timestamptz DEFAULT now()
);
CREATE INDEX idx_moyue_user_sources_status ON moyue_user_sources (status, created_at DESC);
CREATE INDEX idx_moyue_user_sources_tags ON moyue_user_sources USING GIN (tags);

-- 用户对源的举报
CREATE TABLE moyue_source_reports (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  source_id uuid REFERENCES moyue_user_sources ON DELETE CASCADE,
  reporter_id uuid REFERENCES auth.users,
  reason text NOT NULL,
  status text CHECK (status IN ('pending', 'resolved', 'dismissed')) DEFAULT 'pending',
  resolved_by uuid REFERENCES auth.users,
  resolved_at timestamptz,
  created_at timestamptz DEFAULT now()
);

-- 源黑名单（域名 / URL 模式）
CREATE TABLE moyue_source_blacklist (
  pattern text PRIMARY KEY,            -- 域名或 URL 模式
  reason text,
  created_by uuid REFERENCES auth.users,
  created_at timestamptz DEFAULT now()
);

-- RLS
ALTER TABLE moyue_user_sources ENABLE ROW LEVEL SECURITY;
CREATE POLICY "approved sources are public" ON moyue_user_sources FOR SELECT USING (status = 'approved' OR auth.uid() = user_id);
-- ... 其他策略略

-- RPC: 引用计数 +1
CREATE OR REPLACE FUNCTION increment_source_cite(p_source_id uuid)
RETURNS void AS $$
BEGIN
  UPDATE moyue_user_sources SET cite_count = cite_count + 1 WHERE id = p_source_id;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

**UI 位置**（v0.2.0 仍用 readest 风格，不做 UI 提级）：
- 入口：`/opds` 页加"社区源" Tab（与"内置源" / "自定义源"并列）
- 列表：`/opds/community`（源卡片网格）
- 上传：`/opds/community/new`（需登录）
- 详情：`/opds/community/[id]`（源元数据 + 引用按钮 + 举报按钮）
- 管理员：`/admin/sources`（待审 / 全部 / 已下架 三栏）

**合规边界**（**N15 红线**，详见 ADR-2026-09-09-05）：
1. 平台**不预置**任何用户上传源到内置清单
2. 平台**不展示**源指向的具体内容（书 / 封面 / 摘要）
3. 平台**不排行**（不按引用数排序，避免引导）
4. 平台有**审核 + 举报 + 黑名单 + 24h 下架**机制
5. 平台有**免责声明**（用户协议补充）
6. 上传者需**已注册 + 已登录**（邀约制下身份可追溯）

**工程量**：~5-6 天
- 数据库 + RLS + RPC：1 天
- 管理员端：1.5 天
- 用户端 UI：2 天
- 端到端 + 风险控制：0.5-1.5 天

**关键风险**：
- 用户上传指向盗版站（笔趣阁 / zlibrary）
- **缓解**：上传时黑名单扫描 + 引用前二次确认免责声明 + 管理员审核 + 举报下架
- 用户"擦边球"（URL 看似合规但实际指向盗版）
- **缓解**：保留举报通道；管理员定期审计

---

## 六、依赖图

```
N10（独立，3-4 天）
  ↓
N11a（基础解析，5 天）  ← 核心增量
  ↓
N11b（JS 沙箱，4-7 天）  ← 依赖 N11a
  ↓
N12（依赖 N11 源抽象，3-4 天）
  ↓
N15（依赖 N11a + N14，5-6 天）
N14（贯穿 N10-N15，2-3 天，与 N11 同步）
```

**总工期**（并行估算）：~18-25 天
**串行最坏**：~22-29 天
**关键路径**：N11a → N12 → N15

---

## 七、合规红线（写进墨阅项目规则.md）

1. **内置源清单**：仅公版 / CC / 明确授权 — **至少 1 个中文源**（用户决策）
2. **自定义书源**：墨阅不预置 Legado 社区源；不提供"导入 Legado 社区源"入口
3. **JS 沙箱**：quickjs-emscripten + 严格白名单；服务端 schema 校验
4. **服务端代理**：只代理请求 + 元数据缓存，**不缓存**书内容
5. **社区源（N15）**：
   - **不预置**任何用户上传源
   - **不展示**源指向的具体内容（书 / 封面 / 摘要）
   - **不排行**（不按引用数排序）
   - 审核 + 举报 + 黑名单 + 24h 下架
6. **月销 > 50 单触发 N09 合规**

---

## 八、风险

| 风险 | 等级 | 缓解 |
|---|---|---|
| 中文公版源适配器复杂度高 | 中 | 优先做 Wikisource (zh) 跑通，剩余 v0.2.1 |
| JS 沙箱选型失败（安全漏洞） | 中 | 选 quickjs-emscripten（独立运行时，沙箱级别最高）；带完整白名单 |
| 跨源搜索服务端被反爬 | 中 | User-Agent + 限速 + 元数据缓存（不缓存内容） |
| 用户上传源指向盗版站 | 高 | 上传时黑名单扫描 + 引用前免责声明 + 管理员审核 + 举报下架 |
| 用户"擦边球"源 | 中 | 举报通道 + 管理员定期审计 + 引用前免责声明 |
| 工时超期 | 低 | N11 拆 N11a/N11b 分步交付；N15 拆数据库/管理员/用户端/测试分步 |

---

## 九、验证基线

- 任何 commit：`pnpm lint` + `pnpm test`（44 失败不变）
- 每个新内置源：URL 200 + 内容公版 + i18n 键 + 1 fixture
- N10 端到端：5-8 个源在 `/opds` 列表显示 + 各能搜出 1 本书
- N11a 端到端：用户用真实合规源走通搜→看
- N11b 端到端：1 个 JS 增强源走通搜→看 + 沙箱无 escape
- N12 端到端：5 源同时搜，1.5s 内合并返回
- N15 端到端：上传 → 审核 → 展示 → 引用 → 举报 → 下架 全流程走通
- 资金路径未变（N08 不动），无需跑 `/api/pay/notify`
- `pnpm test` 全量仍为 **44 失败 / 预存基线**

---

## 十、里程碑

| 日期 | 里程碑 |
|---|---|
| 2026-09-09 | v0.2.0 PRD 审批通过 ✅ |
| 2026-09-09 | PRD 文档落地（本文档 + 项目主控 + 项目规则同步）✅ |
| 2026-09-13 | N10 完成（5-8 合规章 + **含中文源** + 适配器层）|
| 2026-09-19 | N11a 完成（自定义书源基础）|
| 2026-09-25 | N11b 完成（JS 沙箱）|
| 2026-09-29 | N12 + N14 完成（聚合搜索 + 合规基础）|
| 2026-10-05 | N15 完成（社区分享）|
| 2026-10-08 | v0.2.0 收官；v0.3.0 启动 UI 提级 |

---

## 十一、附录

### A · 候选源 curl 实测结果（2026-09-09）

| 源 | URL | 无 UA | 加 UA | 备注 |
|---|---|---|---|---|
| Project Gutenberg | m.gutenberg.org/ebooks.opds/ | 301 | — | 上游已有 |
| Standard Ebooks | standardebooks.org/feeds/opds | 401 | 待测 | 上游已有 |
| ManyBooks | manybooks.net/opds/index.php | 403 | 待测 | 上游已有 |
| Unglue.it | unglue.it/api/opds/ | 缺 | 待测 | 上游已有 |
| Wikisource (en) | en.wikisource.org (MediaWiki API) | 200 | — | 非 OPDS，需适配器 |
| Wikisource (zh) | zh.wikisource.org (MediaWiki API) | 待测 | — | **必入**（用户决策）|
| Internet Archive | archive.org advancedsearch | 200 | — | 非 OPDS，需适配器 |
| DOAB | doabooks.org | 403 | 待测 | 可能 Cloudflare 拦截 |
| 国家图书馆 | nlc.cn | 200 | — | 待查公版书页（多为 PDF 扫描）|
| 浙江图书馆 | zjlib.cn | 200 | — | 待查公版书页（多为 PDF 扫描）|

### B · 关键参考

- Legado 书源规则文档：https://alanskycn.gitee.io/teachme（合规版）
- quickjs-emscripten 文档：https://github.com/justjake/quickjs-emscripten
- OPDS 规范：https://specs.opds.io/opds-1.2
- MediaWiki API：https://www.mediawiki.org/wiki/API:Main_page

### C · 关联节点状态（待实施时同步）

| 节点 | 状态 | 依赖 |
|---|---|---|
| N10 | ⬜ 未开始 | 无 |
| N11a | ⬜ 未开始 | 无 |
| N11b | ⬜ 未开始 | N11a |
| N12 | ⬜ 未开始 | N11a |
| N14 | ⬜ 未开始 | 与 N10-N15 同步 |
| N15 | ⬜ 未开始 | N11a + N14 |
