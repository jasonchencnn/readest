# 上游 merge 解决记录（2026-09-14）

> **任务**：#8 readest 上游 merge 实施（187 commits）
> **上游目标**：`c3a95ba`（`c3a95ba7b35c013baa644358669d28bd55efaba8`）
> **合并基线（merge-base）**：`4cbb2a2`（`4cbb2a25e7035e17d6a48d7c04eb52a102c9286d`，fork 品牌改名起点）
> **我方 HEAD**：`c8e5c04`（main）
> **合并分支**：`merge/upstream-20260914`（自 `main` 切出）
> **提交策略**：**仅一个 merge commit**，消息 `Merge readest upstream c3a95ba into main (moyue v0.2.0 base)`，**不 push**。
> **纪律**：全程 `git merge --no-commit --no-ff`，保留 `MERGE_HEAD`；未执行 `--abort` / `reset --hard` / `checkout .`。
> **依据**：`合并冲突分类-20260914.md`、`合并冲突勘察-20260914.md`。

---

## 一、执行概览

| 项 | 结果 |
|---|---|
| 冲突总数 | 73（勘察确认） |
| 未合并文件（U） | **0** |
| 分支 | `merge/upstream-20260914`，`MERGE_HEAD=c3a95ba` |
| 暂存变更（相对 main） | 439 新增 / 741 修改 / 124 删除 |
| 冲突解决策略 | A=52、A+=5、B=2、C=13、DU=1、UD=1 |
| lint（`tsc --noEmit && biome lint .`） | **0 error**（2403 文件） |
| 全量单测 | 见 §六（相对 baseline 归因） |

---

## 二、冲突解决（73）

### A 类 · 纯改名（52）——取上游版本 + 逐 hunk 重放品牌改名
- A-1 翻译 **32**：`public/locales/<L>/translation.json`（ar…vi，含 zh-CN/zh-TW 见 A+）。
- A-2 代码/配置 **20**：`Info-ios.plist`、`Info.plist`、`ImportNovelDialog.tsx`、`useBooksSync.ts`、`ExportMarkdownDialog.tsx`、`BooknoteItem.tsx`、`kosyncProgress.ts`、`useKOSync.ts`、`useProgressSync.ts`、`BookOrbitForm.tsx`、`KOSyncForm.tsx`、`LocalSendForm.tsx`、`S3Form.tsx`、`transferManager.ts`、`xcfi.ts`、`providers.test.ts`、`ImportAnnotationsDialog.test.tsx`、`useProgressSync.test.tsx`、`S3Provider.test.ts`、`ImportFromUrlDialog.tsx`（同时是 UD 上游删除项）。
- **重放规则**（由 `git diff 4cbb2a2..main` 实证，仅 lookbehind 边界，避免误改 camelCase 标识符）：
  - `(?<![A-Za-z0-9_])Readest` → `Moyue`（zh-CN/zh-TW 语境 → `墨阅`）
  - `(?<![A-Za-z0-9_])readest` → `moyue`（含 `readest://` → `moyue://`）
  - `(?<![A-Za-z0-9_])READEST` → `MOYUE`；`bilingify` → `moyue`
  - 前置：`com.bilingify.readest` → `com.moyue.app`，`readest-onedrive` → `moyue-onedrive`
  - **保留（KEEP）**：`readest-api.chen-cn.top` / `readest.chen-cn.top`、`readest-access-token`、`cloudflarestorage.com/readest`、`readest/`、`bucket:'readest'`、`readest.com`、`com.readest.`、`READEST-<n>`、`readest_transfer_queue`、`providers/readest`；及标识符 `onImportReadest`、`isReadestCloudEnabled`、`ReadestRuntimeConfig` 等。
- **A 类自检**：`51/52` 逐字节一致；唯一差异 `BooknoteItem.tsx` 为 prettier 折行（3 行→1 行），语义/内容等价（已复核）。

### A+ 类 · 改名 + 我方新增（5）——取上游 + 重放改名 + 叠加我方新增（union）
- `en/translation.json`：追加我方 6 键（`catalog.wikisource-zh/en.*`、`catalog.archive-publicdomain.*`）。
- `zh-CN/translation.json`：改名 59 处 + 我方新增键并入（结果 +58 键）。
- `zh-TW/translation.json`：同上（+58 键）。
- `app/library/page.tsx`：改名 + 保留我方 `useOpenRedeemLink`（`moyue://redeem` 深链）。
- `services/runtimeConfig.ts`：改名 + 保留我方 `paymentProvider`（epay/stripe 运行时切换）。
- 3-way 用 `git merge-file`（U=改名后上游，B'=改名后 base，O=我方）实现；locale 键并集脚本产出合法 JSON。

### B 类 · 独立新增（2）——并集
- `apps/readest-app/package.json`：取上游（`version 0.12.8`）。⚠️ 版本策略见 §七。
- `data/metainfo/appdata.xml`：并集——保留我方 `0.1.0` release 条目 + 全部上游 release（XML 校验通过）。

### C 类 · 功能型（13）——逐文件人工
1. **`README.md`**：保留我方（墨阅速览）整套；弃上游 14 行小改。**（文档，无功能风险）**
2. **`src-tauri/gen/android/app/build.gradle.kts`**：吸收上游 Sentry DSN 块 + `usesCleartextTraffic="true"`；保留 `applicationId="com.moyue.app"` + `storeFlavor`。**（`gen/` 被 gitignore，`git add -f`）**
3. **`src-tauri/gen/android/app/src/main/AndroidManifest.xml`**：以**上游完整 manifest** 为基 + 重放我方 scheme 改名（`readest`→`moyue`、`readest-onedrive`→`moyue-onedrive`）。**注：我方 fork 此前把该文件回退成了最小模板，本次以上游版本为正确基线。**
4. **`app/auth/components/AuthPanel.tsx`**：**保留我方「无 OAuth」**；吸收上游同区域非 OAuth 改动；移除残留 `handleProviderSignIn`/`onProviderSignIn`/`OAuthProvider` 引用（lint 修复）。
5. **`app/user/page.tsx`**：**保留我方 epay 兑换/支付 UI** + 吸收上游 Stripe（`shouldUseBillingPortal`、`openStripePortal`）；`useQuotaStats()` 解构补 `customizationPurchased`；`handleManageSubscription` 改为 epay 感知。
6. **`components/settings/integrations/SendToReadestForm.tsx`**：**保留服务端 plan 解析**（`/api/user/plan`）。
7. **`hooks/useQuotaStats.ts`**：**保留服务端 plan/配额解析**；`customizationPurchased = (userProfilePlan === 'purchase')`；并**吸收上游 sign-out 缓存清理**（见 §五）。
8. **`pages/api/send/address.ts`**：保留 `getUserPlanData`（`@/utils/plan`）。
9. **`pages/api/send/senders.ts`**：保留 `getUserPlanData`。
10. **`pages/api/storage/stats.ts`**：保留 plans 表 quota + `usage=totalSize`。
11. **`pages/api/storage/upload.ts`**：保留服务端配额判定 `getUserPlanData(user.id)`；**恢复**随上游冲突侧一度丢失的 `const supabase = createSupabaseAdminClient();`（第 97 行仍被引用）。
12. **`apps/readest.koplugin/main.lua`**：取**上游大重写**（含 `READER_READY_PULL_DELAY`→`BACKGROUND_PULL_DELAY` 等强制改名）+ **回填自建配置**（`SUPABAE_ANON_KEY_BASE64`、`supabase_url=https://readest-api.chen-cn.top`）。
13. **`src/__tests__/services/send-address-plan-gate.test.ts`**：mock 由 `getUserProfilePlan` → `getUserPlanData`（`@/utils/plan`），与 #8/#9 一致。

> C 类共性：6–11、13 是同一「plan 解析由 JWT claim 改为服务端 `/api/user/plan`（plans 表，N08 会员链路，migration 020）」的改造，7 为其 UI 镜像。**统一按「服务端模型」合并（保留我方）。**

### DU / UD · 删除冲突
- **DU** `app/auth/components/AuthPanel.tsx` 关联的 `ProviderLogin.tsx`（我方删 / 上游改）→ **保留我方删除**，丢弃上游改动。
- **UD** `app/library/components/ImportFromUrlDialog.tsx`（上游删 / 我方改）→ **接受上游删除**（我方此处仅品牌改名，无功能改动）。

---

## 三、merge 期新增的删除项核对（相对 main：124 删除）

| 类别 | 数量 | 结论 |
|---|---|---|
| `src-tauri/**` 自动生成权限 `.toml`（`permissions/autogenerated`，含 native-bridge/tts 属下） | 116 | 上游 tauri 升级**重新生成**命令权限文件（旧名 → 新名）；插件源码（`android/`、`ios/`、`src/`、`tests/`）**完整保留**，非功能删除。 |
| `src/`（`statsSyncMerge.test.ts`、`xcfi.kosync-section-offset.test.ts`、`ImportFromUrlDialog.tsx`、`user/components/PlanIndicators.tsx`、`user/components/PlanNavigation.tsx`） | 5 | 上游重构/删除（UD + 上游移除）。 |
| `apps/readest-app/tailwind.config.ts` | 1 | **未被我方改动**（`diff 4cbb2a2..main` 为空）；上游迁 Tailwind v4，`postcss.config.mjs` 已用 `@tailwindcss/postcss`，删除无 fork 损失。 |
| `packages/tao`、`packages/tauri-plugins` | 2 | 上游移除的 submodule。 |

---

## 四、merge 期「自动合并 / 上游新增文件」引入问题的额外修复

> 冲突清单外，存在若干**自动合并**或**上游新增**文件，它们携带上游品牌或在语义上依赖上游（JWT）模型，导致 `merge` 后出现回归。逐项修复：

### 4.1 上游新增测试的深链 scheme 泄漏（`readest://` → `moyue://`）
- 现象：`useOpenBookLink.test.ts`（3 失败）、`useOpenBookLinkColdStart.test.ts`（5 失败）——钩子 `useOpenBookLink.ts` 已随 merge 取上游逻辑 + 我方 scheme 改名（`moyue://`），但上游**新增**的测试文件仍喂 `readest://`，`parseBookDeepLink` 仅认 `moyue:` → 路由不触发 → mock 未被调用。
- 修复：对 merge 新增的测试文件重放 scheme 改名：`readest://` → `moyue://`：
  - `src/__tests__/hooks/useOpenBookLink.test.ts`
  - `src/__tests__/hooks/useOpenBookLinkColdStart.test.ts`
  - `src/__tests__/utils/deeplinkConsume.test.ts`（本已通过，仅一致性处理）
- 依据：main 中所有既有测试统一使用 `moyue://`（`deeplink-book.test.ts`、`useOpenAnnotationLink.test.ts`、`auth.test.ts` 等），fork 规范为「新代码禁用 `readest://`」。
- 结果：**8 个失败全部转绿**。

### 4.2 translators `providers.test.ts` 的 deepl 断言取错调用
- 现象：`deeplProvider` 5 个失败，`SyntaxError: "undefined" is not valid JSON`。
- 根因：我方 fork 的 `deepl.ts` 在翻译前有**服务端 plan 前置请求** `/user/plan`（fork 语义；main 同款），故 `mockFetch.mock.calls[0]` 是 plan 请求（无 body）；上游**新增**的 `sentBody()` 固定读 `calls[0]`。
- 修复：`sentBody` 改读**最后一次调用**（即携带 body 的 translate POST），断言（语言大小写）保持不变。**非弱化断言**——所校验的上游 `toDeepLLang` 行为我们已采用且代码确会产出。
- 结果：**5 个失败转绿**。

### 4.3 `hooks/useQuotaStats.ts` 吸收上游 sign-out 缓存清理
- 现象：上游 `useQuotaStats` 在 `!user || !token` 时清空模块级 entitlement 缓存（`setCachedUserPlan(undefined)`、`setCachedCustomizationPurchased(false)`），供 `resolveCloudSyncGate` 同步读取；我方（main）无此清理 → 登出后短暂「仍显高级」。merge 整体取我方，导致该上游修复被丢弃。
- 修复：在 `fetchPlanStats` 的早返分支**吸收**上游清理逻辑（plan **解析**仍保持服务端），与上游结构一致、风险极低。

---

## 五、验证结果

| 验证项 | 命令 / 方式 | 结果 |
|---|---|---|
| 冲突清零 | `git diff --name-only --diff-filter=U` | **0** |
| JSON（locales） | 35 个 `public/locales/*/translation.json` | **35 校验通过 / 0 失败** |
| XML（appdata） | `data/metainfo/appdata.xml` | 合法 |
| lint | `tsc --noEmit && biome lint .` | **0 error**（2403 文件） |
| 内容自检（A 类） | 改名重放逐字节比对 | **51/52**（`BooknoteItem.tsx` 仅折行） |
| 全量单测 | `vitest run --maxWorkers=4`（`</dev/null`，避免 watch 陷阱） | 见 §六 |

---

## 六、全量单测 baseline 对比与差异归因

| | 失败文件 | 失败用例 | 总用例 |
|---|---|---|---|
| **baseline（main，合并前）** | 9 | **44** | 9637 |
| **合并后（当前）** | 13 | **55**（其中 2 为机器负载抖动，见下） | 11181 |

> 合并后一次稳定读数（隔离复跑）：**53**。
> `library-search-ssr.test.ts`、`KeyboardShortcutsSettings.test.tsx` 各 1 例为 `Test timed out in 5000ms`——机器高负载抖动；**隔离复跑均通过**（2 文件 5 用例全绿），非回归。

### 差异归因（+11 = 55 − 44）

| 项 | 数量 | 归因 |
|---|---|---|
| `services/translators/providers.test.ts` | 5 → **0** | §4.2 已修复（上游新增 deepl 断言取错调用） |
| `hooks/useOpenBookLink(.ColdStart).test.ts` | 8 → **0** | §4.1 已修复（上游新增测试 scheme 泄漏） |
| `api/storage-upload-quota-freshness.test.ts` | +4 | **上游新增**测试，断言上游「额度取自 JWT claim + `storage_usage_bytes` 现势」模型；我方为**服务端 plan 表**模型（mock 未覆盖 `getUserPlanData` → 500）。**fork 服务端模型胜出，预期失败**。 |
| `hooks/useQuotaStats-signout.test.ts` | +2 | **上游新增**测试，断言 `customizationPurchased` 取自 `getCustomizationPurchased(token)`（JWT）；我方取自服务端 plan（`fetch('/user/plan')`），测试未 mock `fetch`。**fork 服务端模型胜出，预期失败**。 |
| `services/novel/novel-import.test.ts` | +4 | 上游为同一文件**新增用例**，命中**预存在**环境根因（jsdom 下 Blob→`@zip.js` 报 `File format is not recognized`；baseline 6 例同因）。**非我方代码回归，属既有环境限制**。 |
| `android/android-auto-declarations.test.ts` | −1 | 上游修复使 1 例转绿（改善）。 |
| 其余 baseline 失败文件 | 0 | `updater`(14)、`yomitan/importer`(12)、`dict-integration`(3)、`libs/document`(3)、`themed-icon`(2)、`series-metadata`(1)、`document-loader-epub-undeclared-cover`(1) **数量与 baseline 完全一致**。 |

**结论**：除去 2 例机器抖动与 1 例改善，净增 9，**全部**可归因于「上游新增、但编码上游 JWT/`customization_purchased` 模型」的测试（6）与「既有环境限制覆盖到上游新增用例」（4），**无我方功能回归**。

---

## 七、遗留 / 需团队决策项

1. **版本策略（阻断项，待裁决）**：`apps/readest-app/package.json` 当前为上游 **0.12.8**，而 `data/metainfo/appdata.xml` 仍含我方 **0.1.0** release 条目——**二者不一致**。分类文档 §四 B 明确此项「需你决策」。可选：(a) 跟随上游 `0.12.8`（并对齐 appdata）；(b) 保持 moyue 自有 `0.1.0`（回填 package.json）。**当前按上游 0.12.8 落地，未擅自改动。**
2. **残留品牌泄漏（内容一致性，非测试失败）**：merge **新增**文件仍带上游品牌，典型如 `src/app/auth/components/ReadestCloudOptIn.tsx`（其文件名 + 被 `AuthPanel.tsx` 引用 + 同名测试通过）及约 8 个 merge 新增 `src` 文件含 `Readest`（`services/annotation/providers/readera.ts`、`services/notion/NotionClient.ts`、`components/settings/KeyboardShortcutsSettings.tsx` 等）。**与 main 现状同风格（fork 改名是选择性的：文档/原生/锁文件大量保留 `Readest`），未纳入本次冲突解决范围**，建议作为后续清理任务。
3. **上游 JWT 模型测试（`storage-upload-quota-freshness`、`useQuotaStats-signout`）**：如需转绿，须将其 mock 改写为服务端 plan 模型——**属跨越 fork 语义的测试改写**，不宜在 merge 中做。建议保留为「预期失败」并在后续基线中登记。
4. **native 深链 scheme 不一致（main 既有）**：`ReadingWidgetProviders.kt` 等原生代码仍发 `readest://book/{hash}`，而 TS 层 `parseBookDeepLink` 仅认 `moyue:`。此为 **main 现存的既有问题**，非本次 merge 引入，未在本次范围内处理。

---

## 八、提交

- 分支：`merge/upstream-20260914`
- 提交数：**1 个 merge commit**
- 消息：`Merge readest upstream c3a95ba into main (moyue v0.2.0 base)`
- 推送：**否**（按要求不 push）

---

## 九、本次记录涉及的额外修复文件清单

| 文件 | 变更 |
|---|---|
| `apps/readest-app/src/__tests__/hooks/useOpenBookLink.test.ts` | `readest://` → `moyue://` |
| `apps/readest-app/src/__tests__/hooks/useOpenBookLinkColdStart.test.ts` | `readest://` → `moyue://` |
| `apps/readest-app/src/__tests__/utils/deeplinkConsume.test.ts` | `readest://` → `moyue://` |
| `apps/readest-app/src/__tests__/services/translators/providers.test.ts` | `sentBody` 改读最后一次调用（适配 fork `/user/plan` 前置） |
| `apps/readest-app/src/hooks/useQuotaStats.ts` | 吸收上游 sign-out 缓存清理（保留服务端解析） |
