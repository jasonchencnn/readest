# 墨阅（Moyue）项目规则

> 接手者的**唯一权威**项目规则。所有改动、运维、review 都以此为准。
> 关联：`./README.md`（速览）/ `./墨阅改名记录-20260902.md`（阶段实录）/ `./DEPLOY.md`（运维速查）/ `./CHANGELOG-MOYUE.md`（374 处改动索引）。
> 代码仓库：`~/Documents/MyVault/Projects/readest/readest`（base `main@4cbb2a2` 的 fork，含子模块；**6 个 commit 已落地**，未 push；**35 个图标资源改动未 commit**，等 N02 一次性吸收）。
> 服务端：`ssh oracle` → `~/readest/`（官方 clone d49fd8b，比本地新 19 commits；client 跑 `readest-moyue:latest` 本地镜像）。

---

## 一、项目定位

**墨阅 = Readest 的中国定制版 fork**（开源电子书阅读器，EPUB/PDF/多端同步）。原仓 `readest/readest`；我们做了 5 大阶段：品牌改名、邀约登录、会员底座、易支付接入、合规书源。部署在 Oracle Cloud ARM + 官方 Supabase 自托管。已上线 `https://readest.chen-cn.top`。

---

## 二、关键架构决策（review 时先看这 8 条）

1. **服务端配额是唯一信任源**：自托管 GoTrue 无 `plan` JWT claim。客户端 token 不可伪造档位。**所有配额强制点都走 `getUserPlanData`（`src/utils/plan.ts`）**，不要用 `getStoragePlanData`/`getSubscriptionPlan`（这些是 JWT 版，已弃用）。
2. **支付走运行时配置，不走 NEXT_PUBLIC_**：`PAYMENT_PROVIDER` 经 `getServerRuntimeConfig()` → `window.__READEST_RUNTIME_CONFIG.paymentProvider` 注入客户端。`useAvailablePlans`/`page.tsx` 在**调用时**读 `getRuntimeConfig()`（勿在模块顶层求值——SSR 时 window 为 undefined）。
3. **资金路径幂等**：`/api/pay/notify` 验签 → 金额与订单 `amount_cents` **分毫不差** → `mark_order_paid` RPC（条件 UPDATE pending→paid）→ 续期 `plans` → 始终字面量 'success'/'fail'。客户端回跳 `?payment=success` 仅是 UX，**记账真相是 notify**。
4. **深链 scheme 是 `moyue://`**：服务端 + iOS + macOS + Android + 桌面 URL 处理器全部统一。任何生成/解析 URL 的代码必须用 `moyue:`，**新代码禁用 `readest://`/`url.protocol === 'readest:'`**。
5. **数据目录 `Readest` 是有意保留**：见第三.G 节。要改必须同步改 capabilities/default.json、tauri.conf.json assetProtocol、Rust 路径检查。
6. **测试基线**：`pnpm test` 全量 **44 失败 = 预存 fork 状态问题**（updater 的 `UPDATES_DISABLED` 短路、themed-icon/android-auto 的 readestcn 遗留 gen 状态、yomitan/novel/dictionaries/libs-document/series/document-loader 的基线失败）。**改名致损失败 = 0**。任何 commit 都应保持 44 不变；改完跑全量确认。
7. **不搭 ZLibrary 镜像，不代理不存储任何侵权书源**（红线，已写进书源与合规章）。
8. **本地 fork 改动大部分已 commit**：6 个 commit（`14b2502` → `b28abed`）已落地，未 push；35 个 `src-tauri/icons/*` 改动未 commit（等 N02 一次性吸收）。`项目主控.md` 节点 N07 负责 push 收尾。

---

## 三、品牌身份（**单一事实来源**）

| 项 | 值 |
|---|---|
| 中文名 | **墨阅** |
| 英文/拼音 | **Moyue** |
| 显示名规则 | 中文界面/窗口标题/应用名用「墨阅」；拉丁/英文 locale 源 key 用「Moyue」 |
| `productName` / `mainBinaryName` | `moyue` |
| `identifier` | `com.moyue.app` |
| deep-link scheme | `moyue://`、`moyue-onedrive://`（**不是** `readest://`） |
| DBUS_ID | `com.moyue.app` |
| 域名 | 沿用 `readest.chen-cn.top`（不改新域） |
| 邀请码字符表 | Crockford base32 `0-9 A-H J K M N P-T V-Z`（去 I/L/O/U，I/L→1、O→0） |

### 改名触点（命名上「必改」+「不能动」）

**必改**（用户可见/技术身份）：见 `CHANGELOG-MOYUE.md` A 节。

**有意保留（review 时勿触碰）**：

| 类别 | 保留项 | 为什么 |
|---|---|---|
| 数据目录 | `Readest`（`nodeAppService.APP_NAME`、`constants.DATA_SUBDIR`、`sync/file/layout.SYNC_BASE_DIR`） | 非用户可见；改需同步 capabilities/assetProtocol/Rust 路径检查 |
| URL 端点 | `readest.com`/`web.readest.com`/`cdn.readest.com`/`node.readest.com`/`readest.chen-cn.top` | 后端与同步端点，不变 |
| 包名/仓库 | `@readest/*`、`github.com/readest/readest` | 上游保留 |
| 云同步 provider id | `'readest'`（`services/sync/cloudSyncProvider`） | 改会断已存设置 |
| 构建通道 | `DistChannel='readest'` | 内部 |
| 存储键 | `localStorage: readest:lastImportFolder` 等 | 不可见 |
| UTI | `com.readest.fb2`/`com.readest.cbz` | 文件类型标识符 |
| 签名 keystore | `readestcn.keystore` / alias `readestcn` | 旧签名；生产前新建 `moyue.keystore` |

> `gen/android`、`gen/apple` 是生成物——改完 `tauri.conf.json` + `Info-*.plist` 后用 `tauri android init` / `tauri ios init` 重新生成。

---

## 四、产品规格（已实现 + 已部署）

### 4.1 登录（邀约制）
- **链路**：`EmailPasswordAuth` → `POST ${getAPIBaseUrl()}/auth/invite/signup` → 服务端 `validate_invite` → `auth.admin.createUser({email_confirm:true})` → `claim_invite` 原子扣减（失败回滚 `deleteUser`）→ 客户端 `signInWithPassword`
- **错误码契约**（服务端→客户端 `signupErrorToMessage` 映射 i18n）：
  | code | 中文 i18n |
  |---|---|
  | `invalid_request` | 请输入有效的邮箱、密码和邀请码 |
  | `invalid_or_exhausted` | 邀请码无效或已被使用 |
  | `email_exists` | 该邮箱已注册 |
  | `signup_failed` | 注册失败，请稍后重试 |
- **管理端**：`/api/auth/invite/admin`（GET 列表 / POST 生成），Bearer + `ADMIN_EMAILS` 门禁
- **已删**：GitHub/Google/Apple/Discord 社交登录（`ProviderLogin.tsx` 已删，auth 页 OAuth 流程全移除；Google Drive/OneDrive **文件同步** OAuth 保留）
- **运维**：`DISABLE_SIGNUP=true` + `ADMIN_EMAILS=<管理员邮箱>`；首个管理员账号须经 GoTrue admin/Studio 直建

### 4.2 会员档（月订阅，CNY）
| 档 | 空间 | 价格 |
|---|---|---|
| 免费 | 100 MB | 0 |
| Plus | 2 GB | 15 元/月 |
| Pro | 5 GB | 30 元/月 |

- `DEFAULT_STORAGE_QUOTA` + `MEMBERSHIP_PLANS`（`src/services/constants.ts`）；productId `moyue.plus.monthly` / `moyue.pro.monthly`
- **服务端直查**：`getUserPlanData(userId)` 走 3 个 RPC（`get_user_plan` + `get_storage_usage` + plans 行 current_period_end）

### 4.3 支付（**当前路径：兑换码** + 预留易支付 / epay 备用）

> **2026-09-08 方案变更**：原计划接易支付/YunGouOS 第三方支付；改为**完全绕开第三方支付**，走兑换码模式。详见 `项目主控.md` ADR-2026-09-07-02 + `需求-N08-兑换码支付.md`。

#### 4.3.1 当前路径 · 兑换码支付（资金零接触）

- **核心思路**：资金流走闲鱼/微信/支付宝直接转账（买家↔卖家），墨阅不参与；价值路径走兑换码 → 服务端校验 → 写 `plans` 表
- **优势**：0% 费率 / 0 开户费 / 0 合规风险（不碰资金流）
- **业务流**：买家转账 → 卖家（管理员）通过 admin API 生成兑换码 → 卖家把码发给买家 → 买家 `/user` 页输码兑换 → 立即升级
- **服务端路由**：
  - `POST /api/redeem` — 用户兑换（鉴权 + Crockford 规范化 + 调 `redeem_code` RPC）
  - `GET /api/admin/redemption-codes` — 管理员列表（`ADMIN_EMAILS` 门禁）
  - `POST /api/admin/redemption-codes` — 管理员生成（1-100 个码 + 碰撞重试）
- **数据库**：迁移 025（`redemption_codes` 表 + `redeem_code` RPC）—— 见 `CHANGELOG-MOYUE.md` J1
- **客户端 UI**：`/user` 页 `RedemptionCard.tsx`（PlansComparison 下方）+ 11 i18n 键
- **限制**：闲鱼官方对"虚拟卡密"类目有监管；月销 > 50 单触发 N09 合规

#### 4.3.2 备用路径 · 易支付 / epay 协议（**已 commit 但当前未启用**）

- **库** `src/libs/payment/epay.ts`：MD5 签名（过滤 sign/sign_type/空值→ASCII 排序→拼 `&key`→小写 hex）；`buildEpayPayUrl` 对**原始值**签名；`verifyEpayNotify` 只强制 `sign`+`out_trade_no`（submit.php 不带 trade_status）
- **路由** `src/pages/api/pay/`：`create` / `notify`（资金路径） / `return` / `query`（限本人）
- **客户端**：`/user` onSubscribe 三分发（IAP / epay / stripe）；epay 流程 = 渠道选择弹窗 → create → 桌面 `openUrl` 外部 / 其余 `location.href` 跳网关 → 回跳轮询 query → toast + reload
- **环境变量**：`PAY_GATEWAY_URL/ID/KEY`（**当前全部留空** → epay 路由对登录用户回 503，运行时无副作用）、`PAY_SITE_URL`（公开回调域）、`PAYMENT_PROVIDER=epay`（运行时注入）
- **启用场景**：未来如切回第三方支付（如商业化前置、个人收款灰区可接受、或 YunGouOS 个人档费率优于兑换码方案），仅需填 `PAY_GATEWAY_*` 三件套即可
- **架构预留**：官方微信/支付宝直连将来只换 adapter，不动订单表与前端

### 4.4 书源（合规边界）
- **桌面/移动**：书源 = `/opds` OPDS 浏览器（搜索/下载/鉴权/4 合法预设/无限自定义）
- **KOReader**：核心自带 OPDS，readest.koplugin 不重复
- **预设必须可验证**（不得凭空写 URL；新增前实测 feed 可用 + 内容合规）
- **不搭 ZLibrary 镜像、不代理不存储任何侵权内容**

### 4.5 v0.2.0 墨阅书源整合（2026-09-09 启动，PRD 待实施）

> 详细设计见 `./需求-v0.2.0-书源整合.md`（PRD 主文档）。本节作为产品规格补充。

#### 4.5.1 内置合规章清单（v0.2.0 起持续维护）
| 源 ID | 名称 | URL | 类型 | 协议 | 合规依据 | 状态 |
|---|---|---|---|---|---|---|
| `gutenberg` | Project Gutenberg | `https://m.gutenberg.org/ebooks.opds/` | 海外 / 英文 | OPDS | 公共领域 | ✅ v0.1.0 已有 |
| `standardebooks` | Standard Ebooks | `https://standardebooks.org/feeds/opds` | 海外 / 英文 | OPDS | CC0 | ✅ v0.1.0 已有 |
| `manybooks` | ManyBooks | `https://manybooks.net/opds/index.php` | 海外 / 英文 | OPDS | 作者授权 / 公共领域 | ✅ v0.1.0 已有 |
| `unglue.it` | Unglue.it | `https://unglue.it/api/opds/` | 海外 / 英文 | OPDS | 作者"解绑"授权 | ✅ v0.1.0 已有 |
| `wikisource-zh` | 中文维基文库 | `https://zh.wikisource.org/w/api.php` | 海外 / **中文公版古籍** | MediaWiki API（**需适配器**） | CC-BY-SA 3.0 / 公共领域 | ⬜ N10 待实测 |
| `wikisource-en` | 英文维基文库 | `https://en.wikisource.org/w/api.php` | 海外 / 英文 | MediaWiki API（**需适配器**） | CC-BY-SA 3.0 / 公共领域 | ⬜ N10 待实测 |
| `internet-archive-oa` | Internet Archive 公共领域 | `https://archive.org/advancedsearch.php` | 海外 / 多语种 | advancedsearch API（**需适配器**） | 公共领域 / CC | ⬜ N10 待实测 |
| `doab` | DOAB 学术专著 | `https://www.doabooks.org/` | 海外 / 学术 | OPDS（待测 UA） | CC | ⬜ N10 待实测 |

**新增要求（v0.2.0 起）**：
- 每个新源**必须实测** URL 200 + 内容明确公版/CC/授权后才能加入本表
- **至少 1 个中文源**（用户决策"增加中文源"）— 必入 `wikisource-zh`
- 适配器层抽象（`opds` / `mediawiki` / `archive-advancedsearch` / `custom-http` 4 类型）— 避免每个源硬编码

#### 4.5.2 自定义书源（Legado JSON 子集）
- **格式**：墨阅自定义书源 = **Legado JSON 规范子集**（`bookSourceName` / `searchUrl` / `ruleSearch` / `ruleBookName` / `ruleChapterList` / `ruleBookContent` / `ruleToc` / JS 增强）
- **解析引擎**：XPath（`node-html-parser` + `xpath`）/ JSONPath（`jsonpath-plus`）/ CSS 选择器（`cheerio`）
- **JS 沙箱**（N11b）：`quickjs-emscripten`（独立 wasm 运行时）
  - 沙箱边界：超时 5s / 内存 64MB / 禁用 `require` / `process` / `globalThis.fetch` / `fs` / `net` / 动态 `eval` / `Function` 构造
  - 白名单 helper：`httpGet` / `httpPost` / `log` / `base64Encode/decode` / `parseHtml` / `crypto.md5/sha256`
- **不接 Legado 生态**：墨阅不预置 Legado 社区源、不提供"导入 Legado 分享源"入口、不支持 `legado://import/` 协议

#### 4.5.3 跨源聚合搜索
- **架构**：客户端搜索 → 服务端 `/api/search/opds` fan-out → 代理层（UA + 限速 + 元数据缓存 TTL 24h）→ 合并去重（Levenshtein ≤ 3）→ 中文优先排序
- **服务端边界**：只代理请求 + 元数据缓存，**不缓存书内容**到墨阅
- **API 契约**：`GET /api/search/opds?q=&page=` → `{ hits, sources: [{id, name, status, hitCount, timeMs}], totalTime, page, hasMore }`

#### 4.5.4 社区书源分享（N15）
- **核心功能**：用户上传源 → 审核通过 → 展示（**仅元数据**）+ 引用 + 举报
- **数据库**：迁移 026（`moyue_user_sources` / `moyue_source_reports` / `moyue_source_blacklist` 三表 + RLS + 引用计数 RPC）
- **UI 位置**：`/opds/community`（v0.2.0 不做 UI 提级，保留 readest 风格入口）
- **合规边界**详见第五节第 5 条

---

## 五、合规红线（不可越）

1. **不托管盗版书库**（含 ZLibrary 镜像 / Anna's Archive / Library Genesis / Libgen 任何形式）
2. **网络出版/APP 备案**：商业分发图书涉及《网络出版服务许可证》与 APP 备案（ICP），上架前补齐
3. **支付合规**：个人收款码不得经营性收款（2022 央行新规）；第四方聚合属无牌灰区。**邀约制小规模试点可接受**；正式收费/上架前补个体户执照走官方直连
4. **数据目录与 provider id 保留**：见第三节"有意保留"表
5. **v0.2.0 社区书源分享（N15）红线**（2026-09-09 新增，详见 ADR-2026-09-09-05）：
   1. 平台**不预置**任何用户上传源到内置清单（内置清单 = 4 复用 + 1-4 新增；社区源物理隔离）
   2. 平台**不展示**源指向的具体内容（书 / 封面 / 摘要）— **只展示源元数据**（书源名 / URL / 描述 / 标签 / 上传者 / 引用数 / 上传时间）
   3. 平台**不排行**（不按引用数排序，避免引导向热门源）
   4. 平台有**审核 + 举报 + 黑名单 + 24h 下架**机制（迁移 026 三表 + RLS + RPC + 管理员端审核队列）
   5. 平台有**免责声明**（用户协议补充："墨阅社区书源由用户上传，平台不为其内容负责；违规源由用户举报后下架"）
   6. 上传者需**已注册 + 已登录**（邀约制下身份可追溯）
   7. **v0.2.0 通用红线**：不接 ZLibrary / Anna's Archive / Library Genesis / Libgen 任何形式接入（CLI / 适配器 / 浏览器内嵌）；不接 Legado 社区书源市场 / 一键导入 Legado 分享源；不接内嵌浏览器访问任意 URL（含 zlibrary / 盗版站）

---

## 六、客户端显示层遗留（**已修，commit `b28abed`**）

> 2026-09-04 ~ 09-07 间的 commit `b28abed fix(client): resolve plan from /api/user/plan in 2 display-layer sites` 已修下列两处。本节保留作为"修法参考"备查。

| 位置 | 修前 | 修后 | 验证 |
|---|---|---|---|
| `src/components/settings/integrations/SendToReadestForm.tsx:74` | `getUserProfilePlan(token)`（JWT） | 异步 `fetch('/api/user/plan')`，与 `useQuotaStats` 同模式 | `pnpm test` 全量 44 失败不变 |
| `src/services/translators/providers/deepl.ts:41` | 同上 | 同上 | 同上 |

---

## 七、构建 / 签名 / 验证

- **环境**：macOS CLT + Rust；Android 需 `JAVA_HOME`(openjdk@17) / `ANDROID_HOME` / `NDK_HOME`
- **构建**（详见 `DEPLOY.md` 与 `封包记录-20260901.md`）：macOS `pnpm tauri build`；Android `pnpm tauri android build --apk --target aarch64`；Windows `cargo-xwin` 交叉编译
- **服务端镜像**：`~/readest-fork/` → `docker build -t readest-moyue:latest .`（ARM，5–30 min，层缓存复用）→ `.env` 切 `READEST_IMAGE=readest-moyue:latest` → `up -d client`
- **验证必跑**：
  - `pnpm lint`（Biome + tsgo）— 改后必跑
  - `pnpm test` — 改后必跑，确认 44 失败不变（任何新失败都是回归）
  - 服务端探活脚本见 `DEPLOY.md` §7
- **图标**（待办）：1024px 主图 → `pnpm tauri icon` + `tauri android init` 重生成 `gen/`
- **签名 keystore**：当前 `readestcn.keystore`/alias `readestcn`；生产前新建 `moyue.keystore`

---

## 八、仓库内约定 / MiniMaxcode do & don't

**遵守**：
- 上游 `apps/readest-app/AGENTS.md` 与 `.agents/rules/`（test-first / typescript / verification）
- 改动最小原则；批量改名用词边界 `\bReadest\b`
- 新增后端路由的 4 步：建迁移 → 加 RPC（如需）→ 加路由 → 改 server-side 强制点；加客户端 useEffect 时**调用时**读 runtimeConfig
- 资金相关代码改动后必须跑 `/api/pay/notify` 端到端 + 1 分钱真单验证

**避免**：
- ❌ 在客户端信任 JWT 里的 plan（自托管无 claim，攻击者可改 token 提升档位 → 服务端直查是唯一信任源）
- ❌ 用 `NEXT_PUBLIC_*` 给 client 注入运行时常量（构建期内联、镜像换不换值都改；用 runtimeConfig 走 window 注入）
- ❌ 改数据目录 `Readest` 或云同步 provider id（破坏已存用户设置）
- ❌ 写未经核实的书源 URL（合规）
- ❌ 改未触动的文件（PR 噪音；定位 bug 用 grep + git blame 而不是改无关文件）
- ❌ 跳过 `pnpm test` 提交（任何新失败 = 回归；44 预存是稳定基线）

**MiniMaxcode 上手第一步**：读 `README.md` → `墨阅项目规则.md`（本文件）→ `CHANGELOG-MOYUE.md`（定位改动）→ `DEPLOY.md`（任何服务端操作前必看）。
