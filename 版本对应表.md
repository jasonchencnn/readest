# 版本对应表（墨阅 ⇄ readest 上游）

> 墨阅（Moyue）**自有版本线独立编号**，与 readest 上游版本通过下表关联；上游仅作为「基线」并入，不决定墨阅版本号。

## 一、一句话说明

墨阅版本号由发布决策独立维护（当前 **0.1.0**），每次并入 readest 上游时，只更新本表中的「上游版本 / 上游 ref / 日期」，墨阅版本号是否递增由发布决策决定。

## 二、映射表

| 墨阅版本 | 上游 readest 版本 | 上游 ref（commit） | 对应日期 | 说明 |
|---|---|---|---|---|
| `0.1.0` | `0.12.1` | `4cbb2a2` | 2026-08-08（上游 0.12.1 发布、我方 fork 起点） | 墨阅初版基线 |
| `0.1.0` | `0.12.8` | `c3a95ba` | 2026-09-14（合并日） | 跳过 0.12.6、直接并入 0.12.8；墨阅版本线不变，仅更新上游基线 |

## 三、版本字段落点（保持一致：均为 `0.1.0`）

| 位置 | 值 | 说明 |
|---|---|---|
| `apps/readest-app/package.json` → `version` | `0.1.0` | **版本真源** |
| `apps/readest-app/src-tauri/tauri.conf.json` → `version` | `"../package.json"` | 跟随 package.json，无需手改 |
| `apps/readest-app/src-tauri/Cargo.toml` → `version` | `0.1.0` | Rust 包版本，与 package.json 对齐 |
| `data/metainfo/appdata.xml` → 最新 `<release>` | `0.1.0` | 其下方 `0.12.8/0.12.6/0.12.1…` 为**上游历史 release**，属正常历史，保留不动 |

**机器可读值（以之为准）**：`apps/readest-app/package.json` 的
- `moyue.upstreamBase` — 当前上游基线版本（如 `"0.12.8"`）
- `moyue.upstreamRef` — 当前上游基线 commit（如 `"c3a95ba"`）

## 四、维护规则

1. 每次并入上游后，**只更新**本表与 `package.json` 的 `moyue.upstreamBase` / `moyue.upstreamRef`（及日期/上游版本列）。
2. 墨阅版本号（`version`）**不随上游变化**；是否递增由发布决策决定（递增时需同步 `Cargo.toml` 与 `appdata.xml`）。
3. 机器可读值以 `apps/readest-app/package.json` 的 `moyue.*` 字段为唯一权威；本文档为人类可读说明，二者应保持一致。
